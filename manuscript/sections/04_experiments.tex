\subsection{Experimental Setup}
\subsubsection{Datasets}
We evaluate FedFairGNN on three real-world public datasets commonly used for financial and graph fraud detection:
\begin{itemize}
    \item \textbf{YelpChi} \cite{rayana2015collective}: A bipartite review graph where spam reviews are treated as fraud. The sensitive attribute $S$ represents the reviewer's popularity or rank, capturing potential platform bias toward established users.
    \item \textbf{Amazon} \cite{mcauley2013amateurs}: A user-item interaction graph for detecting malicious buyers. The sensitive attribute $S$ represents user activity duration, identifying bias against new versus veteran users.
    \item \textbf{Elliptic} \cite{weber2019anti}: A large-scale Bitcoin transaction graph where illicit transactions are labeled as fraud. The sensitive attribute $S$ corresponds to the transaction timestep split (early vs. late).
\end{itemize}
To evaluate the federated setting, the data is partitioned non-IID across $K=3$ clients using a Dirichlet distribution ($\alpha=0.5$).

\subsubsection{Baselines}
We compare FedFairGNN against three state-of-the-art baselines adapted for the federated setting:
\begin{itemize}
    \item \textbf{FraudGNN-RL} \cite{dou2020enhancing}: Utilizes a reinforcement learning agent to selectively aggregate neighbors, filtering out camouflaged fraudsters.
    \item \textbf{GNN-CL} \cite{sun2021dual}: A contrastive learning framework that maximizes the agreement between local and global views to handle label scarcity.
    \item \textbf{Attn-Ensemble}: An attention-gated ensemble model combining a GNN branch and an MLP branch to robustly handle heterophilous graphs.
\end{itemize}

\subsubsection{Implementation Details}
Experiments were conducted on a single machine simulating the federated network.
\begin{itemize}
    \item \textbf{Model Architecture}: 3-layer GAT backbone with $d_{hidden}=128$ and 4 attention heads.
    \item \textbf{Federated Training}: $T=100$ rounds, $E=5$ local epochs, batch size 512.
    \item \textbf{Fairness \& Privacy}: $\beta$ initialized to 0.5, clipped to $[0, 5]$. DP parameters $\epsilon=1.0, \delta=10^{-5}, C=1.0$. Fairness budget $\tau=0.05$.
    \item \textbf{Optimization}: AdamW optimizer with $\eta_{local}=0.01$. BFWA dual learning rate $\eta_{\mu}=0.1$.
\end{itemize}

\subsection{Results}
Table \ref{tab:auc} and Table \ref{tab:dpd} summarize the performance.

\begin{table}[htbp]
\caption{AUC-ROC Comparison (Higher is better)}
\begin{center}
\begin{tabular}{lcccc}
\toprule
Dataset & FedFairGNN & FraudGNN-RL & GNN-CL & Attn-Ensemble \\
\midrule
YelpChi & 0.98 & 0.84 & \textbf{0.99} & 0.97 \\
Amazon & 0.85 & 0.41 & \textbf{0.93} & 0.75 \\
Elliptic & \textbf{0.97} & 0.99 & 0.83 & 0.96 \\
\bottomrule
\end{tabular}
\label{tab:auc}
\end{center}
\end{table}

\begin{table}[htbp]
\caption{Fairness (DPD) Comparison (Lower is better)}
\begin{center}
\begin{tabular}{lcccc}
\toprule
Dataset & FedFairGNN & FraudGNN-RL & GNN-CL & Attn-Ensemble \\
\midrule
YelpChi & \textbf{0.01} & 0.06 & 0.03 & 0.08 \\
Amazon & \textbf{0.06} & 0.07 & 0.04 & 0.10 \\
Elliptic & 0.07 & \textbf{0.02} & 0.03 & 0.04 \\
\bottomrule
\end{tabular}
\label{tab:dpd}
\end{center}
\end{table}

\subsection{Analysis}
\subsubsection{Utility-Fairness Trade-off}
FedFairGNN demonstrates a superior trade-off across the evaluated environments. On \textbf{YelpChi}, FedFairGNN achieves an exceptional fairness score (DPD of 0.01) while maintaining an AUC of 0.98, nearly matching the baseline GNN-CL (0.99 AUC) which has higher bias (0.03 DPD). On \textbf{Elliptic}, it achieves an AUC of 0.97, significantly outperforming GNN-CL (0.83) while providing competitive fairness guarantees via the BFWA constraint.

\subsubsection{Effectiveness of FSER}
On \textbf{Amazon}, where homophily is strong, standard baselines like Attn-Ensemble suffer (AUC 0.75). FedFairGNN achieves 0.85, indicating that FSER effectively filters out biased or noisy edges that mislead standard attention mechanisms.

\subsubsection{Convergence and Stability Analysis}
To better understand the training dynamics, we visualize the convergence of FedFairGNN compared to the strongest baseline, GNN-CL, over the communication rounds.

\begin{figure}[htbp]
\centerline{\includegraphics[width=0.95\columnwidth]{figures/auc_curve.pdf}}
\caption{AUC-ROC progression across federated communication rounds. FedFairGNN maintains stable learning dynamics comparable to non-private non-fair baselines.}
\label{fig:auc_curve}
\end{figure}

\begin{figure}[htbp]
\centerline{\includegraphics[width=0.95\columnwidth]{figures/dpd_curve.pdf}}
\caption{Demographic Parity Difference (DPD) over communication rounds. BFWA successfully suppresses the parity gap below the $\tau=0.05$ threshold.}
\label{fig:dpd_curve}
\end{figure}

As shown in Fig. \ref{fig:auc_curve}, FTGD ensures that the task-specific gradients are largely unperturbed, allowing the global model's AUC to converge smoothly without experiencing the severe variance drops typically associated with pure DP-FedAvg. Concurrently, Fig. \ref{fig:dpd_curve} illustrates the efficacy of the Bi-Objective Frank-Wolfe Aggregation (BFWA) at the server scaling down the fairness constraint violation effectively as training progresses, demonstrating that the global model progressively adheres to the fairness budget without sacrificing significant task utility.
