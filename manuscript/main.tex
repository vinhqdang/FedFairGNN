%% Springer Nature LaTeX template (sn-jnl), official Dec 2024 package.
%% Math and Physical Sciences Numbered Reference Style (sn-mathphys-num).
%% Prepared for submission to a Springer Nature journal (collection:
%% "Trustworthy and Responsible Federated Learning").
\documentclass[pdflatex,sn-mathphys-num]{sn-jnl}

\usepackage{graphicx}%
\usepackage{multirow}%
\usepackage{amsmath,amssymb,amsfonts}%
\usepackage{amsthm}%
\usepackage{mathrsfs}%
\usepackage[title]{appendix}%
\usepackage{xcolor}%
\usepackage{textcomp}%
\usepackage{manyfoot}%
\usepackage{booktabs}%
\usepackage{algorithm}%
\usepackage{algorithmicx}%
\usepackage{algpseudocode}%
\usepackage{listings}%
\usepackage{tikz}%
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,calc,fit,backgrounds}
\usepackage{subcaption}%

\theoremstyle{thmstyleone}%
\newtheorem{theorem}{Theorem}%
\newtheorem{proposition}[theorem]{Proposition}%
\theoremstyle{thmstyletwo}%
\newtheorem{remark}{Remark}%
\theoremstyle{thmstylethree}%
\newtheorem{definition}{Definition}%

\raggedbottom

\newcommand{\dpd}{\mathrm{DPD}}
\newcommand{\eod}{\mathrm{EOD}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\E}{\mathbb{E}}
\DeclareMathOperator*{\argmin}{arg\,min}
\DeclareMathOperator*{\argmax}{arg\,max}
\providecommand{\doiurl}[1]{\url{https://doi.org/#1}}

\begin{document}

\title[Trustworthy Federated Graph Neural Networks]{Trustworthy Federated Graph
Neural Networks: Unifying Fairness, Privacy, Robustness, and Explainability for
High-Stakes Risk Detection}

\author[1]{\fnm{Quang-Vinh} \sur{Dang}}\email{vinh.dq4@buv.edu.vn}

\author[2]{\fnm{Ngoc-Son-An} \sur{Nguyen}}\email{annns25871@pgr.iuh.edu.vn}

\author[3]{\fnm{Dat} \sur{Le}}\email{datla@uef.edu.vn}

\author*[4]{\fnm{Minh Ngoc} \sur{Dinh}}\email{minh.dinh@maeducation.com}

\affil[1]{\orgname{British University Vietnam}, \orgaddress{\city{Hung Yen}, \country{Vietnam}}}

\affil[2]{\orgname{Industrial University of Ho Chi Minh City}, \orgaddress{\city{Ho Chi Minh City}, \country{Vietnam}}}

\affil[3]{\orgname{University of Economics and Finance}, \orgaddress{\city{Ho Chi Minh City}, \country{Vietnam}}}

\affil*[4]{\orgname{Millennia Education}, \orgaddress{\city{Ho Chi Minh City}, \country{Vietnam}}}

\abstract{
Federated Learning (FL) enables institutions to train shared models over
sensitive, decentralised graph data---transactions, credit records, social
ties---without exchanging raw data. Yet accuracy alone does not make such
systems deployable: recent regulatory and scientific frameworks (the EU AI Act,
the NIST AI Risk Management Framework) demand that automated decision systems be
simultaneously \emph{fair}, \emph{private}, \emph{robust}, and
\emph{transparent}. Existing work addresses these properties in isolation, and
no prior method combines fairness, differential privacy, and Byzantine
robustness for federated graph neural networks (GNNs). We present
\textbf{TrustFedGNN}, a unified trustworthy federated graph-learning framework
built from three components: (i) \emph{Fairness-Sensitive Edge Reweighting}
(FSER), which suppresses the biased cross-group message passing responsible for
structural discrimination; (ii) \emph{Fairness-Targeted Gradient Decomposition}
(FTGD), which applies differential privacy to the \emph{released fairness
statistic} by privatising only the low-dimensional demographic-parity means---so
strong DP for the fairness signal costs $O(1)$ noise where full-gradient DP-SGD
must inject $O(\sqrt{|\theta|})$ and destroys utility (we state precisely what
this guarantee covers, and what it does not, in the text); and
(iii) \emph{Bi-objective Frank--Wolfe Aggregation} (BFWA) and its
Byzantine-robust variant, which impose an operator-chosen constraint on the
aggregated client-disparity statistic while screening malicious clients,
including a new fairness-poisoning threat. We further instrument the framework
with predictive uncertainty, GNN attention audits, a composite trust score,
sustainability accounting, and an EU~AI~Act / NIST~RMF compliance mapping.
Across four demographic benchmarks (German, Credit, Bail, Pokec-z) and
two large-scale scalability studies (Elliptic Bitcoin, 203{,}769 nodes; ogbn-products, 2.4M nodes),
TrustFedGNN does not dominate every individual metric, but \emph{to our knowledge},
it is the only method among sixteen baselines to combine competitive fairness and utility with a
statistic-level sensitive-attribute DP guarantee and Byzantine robustness in a single
federated GNN under simulated cross-silo partitions. All tables and
figures are regenerated from logged runs and the code is released, so every
reported number is traceable to a configuration. This work supports SDG~9 by
advancing trustworthy collaborative AI.
}

\keywords{Federated learning, Graph neural networks, Algorithmic fairness,
Differential privacy, Byzantine robustness, Explainable AI, Trust metrics,
Regulatory compliance}

\maketitle

\section{Introduction}
\label{sec:intro}

\noindent Consider a consortium of banks that together want to catch money
laundering. Fraud is fundamentally \emph{relational}: illicit funds move through
chains of accounts, shell companies, and intermediaries, and it is precisely the
\emph{structure} of these money flows---not any single transaction in
isolation---that betrays criminal intent. Graph Neural Networks (GNNs) have
therefore become the tool of choice for financial crime detection, credit
scoring, and other high-stakes risk assessments, because their message-passing
mechanism learns directly from the web of relationships between users, accounts,
and transactions \cite{kipf2016semi,velickovic2017graph,dou2020enhancing}. Yet
the very banks that would benefit most from a shared model are legally forbidden
from pooling their customers' data: privacy regulations such as the GDPR, and
plain commercial confidentiality, keep each institution's transaction graph
locked inside its own walls. Federated Learning (FL) offers a way out. By
exchanging model updates instead of raw data, a group of institutions can train
a single, more powerful detector without any of them surrendering its records
\cite{mcmahan2017communication}.

This picture, however, is dangerously incomplete. A model that is merely
\emph{accurate} is no longer fit to be deployed in decisions that shape people's
access to credit, liberty, or financial services. Three converging forces---the
European Commission's Ethics Guidelines for Trustworthy AI, the EU AI Act, and
the U.S.\ NIST AI Risk Management Framework---now demand that automated
decision systems be---depending on their risk classification, the provider's
role, and the jurisdiction---\emph{fair}, \emph{privacy-preserving},
\emph{robust}, \emph{transparent}, and \emph{accountable}. For systems that fall
under the EU AI Act's high-risk category, several of these move from good
practice toward regulatory obligation (subject to the Act's scope and
conformity-assessment provisions, which we do not attempt to adjudicate here).
Meeting them is hard even for a centralised model. In the federated,
graph-structured setting they collide, and each one becomes markedly harder than
in isolation.

\paragraph{Why the federated graph setting is uniquely hard.}
The difficulty is not that we must bolt four independent features onto a
classifier; it is that the natural solution to each requirement actively
sabotages the others. \emph{Fairness} is confounded by the graph itself:
homophilous message passing means that a member of a demographic group who
happens to be connected to risky neighbours will be flagged as risky too, so the
model learns to launder a protected attribute through graph structure---and the
central server, which never sees any data, cannot even measure this bias, let
alone correct it. \emph{Privacy} appears to have a textbook answer,
differentially private SGD, but that answer fails badly here: adding calibrated
noise to every coordinate of the gradient injects perturbation whose magnitude
grows with the square root of the model dimension, and on the compact GNNs used
for tabular financial graphs this noise simply drowns the signal, collapsing
accuracy to little better than chance (we quantify this in
Section~\ref{sec:exp-privacy}). \emph{Robustness} is threatened not only by the
classical malfunctioning or malicious client, but---as we show---by a subtler
adversary that the fairness machinery itself invites: a client can transmit a
deliberately biased model while \emph{reporting} exemplary fairness statistics,
thereby hijacking any aggregator that rewards fairness. And \emph{transparency}
is undermined by the opacity of relational models, exactly when regulation
requires that each adverse decision be explainable and auditable.

\paragraph{The gap.}
The research literature has, understandably, attacked these problems one at a
time. Fair-GNN methods deliver impressive demographic parity but assume all data
sits in one place \cite{dai2021say,yang2024fairsin,lee2025dabgnn}. Fair federated
learning has matured for tabular and image data but rarely touches graphs
\cite{ezzeldin2023fairfed,li2020fair}. Byzantine-robust aggregation hardens FL
against poisoning but is oblivious to fairness \cite{blanchard2017krum,
yin2018byzantine}. Differentially private FL protects data but, as noted, at a
utility cost that is prohibitive for graph models. To the best of our knowledge,
\emph{no} existing method delivers fairness, differential privacy, and Byzantine
robustness together for federated GNNs, and none has been validated on a
large-scale cryptocurrency graph---arguably the most consequential modern fraud
domain. Closing this gap is the goal of this paper.

\paragraph{Our approach.}
We present \textbf{TrustFedGNN}, a single framework in which fairness, privacy,
robustness, and transparency are designed to reinforce rather than undermine one
another. Its architecture (Fig.~\ref{fig:architecture}) intervenes at the three
natural control points of federated graph training---inside message passing, at
the local gradient, and at server aggregation---and wraps the result in an
instrumentation layer that measures, explains, and audits trustworthiness. The
key technical insight, and the reason privacy becomes almost free, is a change
of perspective on \emph{where} the sensitive attribute actually influences
learning: it does so only through a two-dimensional summary statistic, so we can
protect that statistic directly instead of blindly noising the entire gradient.

\medskip
\noindent Concretely, our contributions are:
\begin{enumerate}
\item \textbf{Privacy-Aware Fairness Feedback Loop (C1).} We introduce a closed-loop co-design coupling Fairness-Targeted Gradient Decomposition (FTGD, Section~\ref{sec:ftgd}) with Bi-objective Frank--Wolfe Aggregation (BFWA, Section~\ref{sec:bfwa}). FTGD establishes that sensitive attributes influence the global fairness objective solely through low-dimensional demographic-parity means, enabling rigorous $(\epsilon,\delta)$-differential privacy over the released fairness statistics at $O(1)$ noise without the catastrophic $O(\sqrt{|\theta|})$ utility collapse of full-gradient DP-SGD. When differential privacy is active (\texttt{dp\_mode='ftgd'}), each client releases the noised disparity statistic $\widetilde{\mathrm{DPD}}_k = |\tilde\mu_0 - \tilde\mu_1|$ actually produced by its local FTGD training step, and BFWA and the other metadata-consuming aggregators operate on this privatised signal; without DP, clients report the (un-noised) validation-split DPD. BFWA's penalised Frank--Wolfe program then steers global aggregation \emph{toward} an operator-chosen disparity budget $\tau$ on these reported client statistics, with per-round feasibility reported as a residual rather than guaranteed (Section~\ref{sec:bfwa}).

\item \textbf{Byzantine-Robust Fairness Aggregation (C2).} We uncover and defend against a deceptive fairness-poisoning threat model where adversarial clients fabricate zero-disparity statistics while transmitting biased or corrupted model updates (Section~\ref{sec:exp-robust}). To defend against this, we develop \texttt{robust\_bfwa}, pairing coordinate-wise distance screening with constrained dual reweighting. At the client level, Fairness-Sensitive Edge Reweighting (FSER, Section~\ref{sec:fser}) provides structural regularization by learning attention corrections that suppress biased cross-group edges, preventing topological bias amplification before local updates are computed.

\item \textbf{Multi-Dimensional Trustworthiness Evaluation \& Empirical Grounding (C3).} We conduct an extensive empirical audit across four demographic benchmarks (German, Credit, Bail, Pokec-z) and two large-scale scalability studies (Elliptic Bitcoin, $203{,}769$ transactions; ogbn-products, $2.4\text{M}$ nodes) against sixteen baselines (Section~\ref{sec:experiments}). Our evaluation provides rigorous ablation grids, multi-seed Byzantine robustness sweeps, an adaptive stealth adversary breakdown analysis, non-IID Dirichlet and topological community scalability tests, 2,000-configuration Monte Carlo trust score rank stability, centralized sanity anchors, and non-demographic operational subgroup audits, fully integrated with EU~AI~Act and NIST~RMF compliance artifacts (Section~\ref{sec:trust}).
\end{enumerate}

\paragraph{Relation to a preliminary version.}
A short preliminary version of this work, introducing the FSER, FTGD, and BFWA
components under the name FedFairGNN, appeared as a conference paper
\cite{dang2026fedfairgnn} evaluated on three fraud-detection graphs
(YelpChi, Amazon, Elliptic) with a single $K{=}3$-client configuration and no
robustness, uncertainty, explainability, or regulatory-compliance analysis. This
article substantially extends that preliminary study: it (i) evaluates on five
standard fairness benchmarks (German, Credit, Bail, Pokec, Elliptic) plus a
$2.4$M-node scalability study (ogbn-products), only one of which overlaps with
the earlier evaluation; (ii) compares against sixteen baselines, including five
2025--2026 methods reimplemented for this comparison, versus the small baseline
set of the preliminary version; (iii) introduces the Byzantine-robust
\texttt{robust\_bfwa} variant and a new fairness-poisoning attack, entirely
absent from the earlier work; (iv) adds an adversarial evaluation of the privacy
mechanism (an attribute-inference attack, Section~\ref{sec:exp-privacy}) and
multi-seed statistical significance testing, neither of which the preliminary
version performed; and (v) adds the full trustworthiness instrumentation
layer---predictive uncertainty and calibration, attention-based audits, a
composite trust score, sustainability accounting, and the EU~AI~Act / NIST~AI~RMF
design mapping (Section~\ref{sec:trust})---which did not appear in the
preliminary version at all. We also renamed the framework from FedFairGNN to
TrustFedGNN to reflect this broadened scope.

By lowering the barrier to trustworthy, infrastructure-grade collaborative AI,
this work contributes to UN Sustainable Development Goal~9 (industry,
innovation, and resilient infrastructure). The remainder of the paper is
organised as follows. Section~\ref{sec:related} situates our work within the
fairness, privacy, and robustness literatures; Section~\ref{sec:prelim}
formalises the federated graph setting and the trustworthiness objectives;
Section~\ref{sec:method} develops the TrustFedGNN framework component by
component; Section~\ref{sec:experiments} reports the empirical study;
Section~\ref{sec:trust} analyses trustworthiness holistically and maps the
system to regulation; and Section~\ref{sec:discussion} discusses implications,
limitations, and broader impact before we conclude.

\section{Related Work}
\label{sec:related}

\paragraph{Fairness in graph neural networks.}
GNNs inherit and amplify bias through homophilous message passing. Remedies
include adversarial debiasing (FairGNN \cite{dai2021say}), counterfactual and
stability regularisation (NIFTY \cite{agarwal2021nifty}), pre-processing that
minimises inter-group distributional distance (EDITS \cite{dong2022edits}),
sensitive-information neutralisation via heterogeneous neighbours (FairSIN
\cite{yang2024fairsin}), re-balancing through counterfactual mixup (FairGB
\cite{li2024fairgb}), invariant learning across inferred sensitive partitions
(FairINV \cite{zhu2024fairinv}), and disentangled debiasing (DAB-GNN
\cite{lee2025dabgnn}). All assume \emph{centralised} data. Our FSER is closest
in spirit to the edge-reweighting variants of FairSIN and to distribution-aware
reweighting \cite{fnrgnn2025}, but operates inside a federated, private, and
robust pipeline.

\paragraph{Fairness in federated learning.}
Two notions coexist. \emph{Client (performance) fairness} equalises accuracy
across clients: q-FedAvg \cite{li2020fair} up-weights high-loss clients.
\emph{Group fairness} equalises outcomes across demographic groups: FairFed
\cite{ezzeldin2023fairfed} adjusts aggregation weights by each client's local
fairness gap; FedFB ports FairBatch to FL; PUFFLE \cite{corbucci2024puffle}
blends a demographic-parity loss with DP-SGD under a target-disparity
controller; FedFACT \cite{fedfact2025} gives provable global-and-local group
fairness via a Lagrangian saddle point over a shared global dual variable and
per-client local duals; Bendoukha et al.\ \cite{bendoukha2025popets}
(PoPETs'25) make FairFed's weighting homomorphically computable, replacing its
$\exp(-\beta|\cdot|)$ term with an FHE-friendly degree-2 polynomial and
aggregating under threshold CKKS; and EquFL \cite{yu2026equfl} (2026) adds a
server-side fairness-gradient correction on top of an arbitrary aggregation
rule, provably compatible with Byzantine-robust ones. These target tabular or
image data. For
\emph{graphs}, F$^2$GNN \cite{f2gnn2023} combines a fairness-weighted
aggregation with structure-aware group-balance weights. The most recent
efforts sit closest to our setting: FairGFL \cite{fairgfl2026} adds DP to
federated fair GNNs but targets \emph{cross-client} (performance) fairness
arising from imbalanced overlapping subgraphs rather than demographic group
fairness; FedGraph-Fair \cite{fedgraphfair2026} applies distributionally-robust
optimisation for personalised group-fair federated graph learning; FaVGNN
\cite{wang2026favgnn} (2026) achieves group fairness in \emph{vertical} FL via
completion-driven adversarial fusion; and FDP-Fair \cite{xue2026fdpfair} (2026)
combines federated DP training with demographic-parity post-processing. A
preliminary version of this work by two of the present authors
\cite{dang2026fedfairgnn} introduced the FSER, FTGD, and BFWA components under
the name FedFairGNN on a smaller evaluation without a robustness, uncertainty,
or compliance analysis; this article is a
substantial
extension, detailed in Section~\ref{sec:intro}. We
compare against FairFed, q-FedAvg, FedFB, F$^2$GNN, FairSIN (run federated),
the strongest 2026 competitors (horizontal adaptations of FaVGNN and FDP-Fair),
and---reimplementing each paper's core statistical mechanism to the extent it
transfers to a single-graph, star-topology federation
(Appendix~\ref{app:baselines})---FairGFL, FedGraph-Fair, PUFFLE, FedFACT, and
the PoPETs'25 FairFed variant, none of which had a public head-to-head against
a fairness-aware federated \emph{GNN} prior to this comparison.

\paragraph{Differential privacy in FL.}
DP-FedAvg \cite{mcmahan2018learning} clips per-client updates and adds Gaussian
noise; privacy is tracked with the moments accountant / R\'enyi DP
\cite{mironov2017renyi,abadi2016deep}. On the compact GNNs used for tabular
graphs, isotropic gradient noise at meaningful $\epsilon$ overwhelms the signal
(Section~\ref{sec:exp-privacy}). FedFDP \cite{ling2024feddp} adapts clipping to
fairness but still privatises the whole gradient, and CE-FedGNN
\cite{guo2026cefedgnn} (2026) instead targets communication efficiency under a
metric-DP guarantee for federated GNN training---neither touches the fairness
statistic specifically. FTGD instead privatises only the \emph{sufficient
statistics} of the fairness term, exploiting the fact that the sensitive
attribute never enters the task loss---a targeted mechanism whose
$(\epsilon,\delta)$ guarantee follows from the Gaussian mechanism and
post-processing immunity. A further 2026 result is cautionary: locally private
GNNs remain vulnerable to structure-inference adversaries even under a
formal $\epsilon$ budget \cite{varun2026adversarial}, which is why we couple
FTGD's privacy guarantee with the robust aggregator (BFWA/robust-BFWA) rather
than treating DP alone as a sufficient defence.

\paragraph{Byzantine-robust aggregation.}
Krum and Multi-Krum \cite{blanchard2017krum}, coordinate-wise median and
trimmed mean \cite{yin2018byzantine}, Bulyan \cite{mhamdi2018hidden}, and
trust-bootstrapping schemes such as FLTrust \cite{cao2021fltrust} defend
against malicious updates under attacks including sign-flipping,
model-replacement \cite{bagdasaryan2020backdoor}, ALIE \cite{baruch2019little},
and IPM \cite{xie2019fall}. None targets fairness. Recently, fairness-poisoning
attacks emerged---EAB-FL \cite{eabfl2024} and the fairness-constrained
optimisation attack \cite{kasyap2025attack}---that \emph{increase} disparity
while preserving accuracy, evading both robust and fairness-aware defences. We
reproduce the fairness-constrained optimisation attack of
\cite{kasyap2025attack} and defend with a robust fairness-constrained
aggregator. Two further 2026 advances harden aggregation against stronger
threat models without touching fairness: CQSA \cite{nath2026cqsa} clusters
updates before a quantum-secure aggregation step, and a post-quantum
lattice-based scheme \cite{rahmati2026pqbyzantine} combines adaptive weighting
with CRYSTALS-Kyber encryption for critical-infrastructure deployments; both
are, like their predecessors, fairness-blind.

\paragraph{Explainability, uncertainty, and trust metrics.}
GNN explainers attribute predictions to edges/features; we use the FSER
attention itself as a native, inspectable audit signal (not a validated
prediction-level explanation) and add gradient-based attribution of the
\emph{disparity}. Predictive uncertainty via MC-dropout
\cite{gal2016dropout} and calibration (ECE, Brier) quantify confidence
reliability; a concurrent 2026 approach, FedWQ-CP \cite{nguyen2026conformal},
instead calibrates conformal-prediction sets in a single round under dual
(statistical and system) heterogeneity---a complementary, distribution-free
alternative to our MC-dropout-based calibration that we discuss as future work
(Section~\ref{sec:discussion}). Composite trust scoring for FL is nascent; we
contribute a transparent power-mean aggregation over utility, fairness,
privacy, robustness, and calibration. On the compliance side, CryptoFair-FL
\cite{ali2026cryptofair} (2026) uses homomorphic encryption to verify
demographic parity and equalised odds without exposing sensitive attributes,
explicitly mapping to EU AI Act Articles~10 and~13---independent confirmation
that regulatory-grounded, cryptographically verifiable fairness auditing is an
active 2026 research direction, though it does not integrate Byzantine
robustness or an explainability layer as we do. Finally, we align the system
to the EU AI Act and NIST AI RMF, operationalising accountability as an
auditable artifact rather than prose.

\paragraph{Positioning.}
Table~\ref{tab:positioning} places the most relevant methods on the five axes
that define our problem. The pattern is stark: the fair-GNN methods are strong
on graph fairness but centralised; the fair-FL methods federate but ignore graph
structure; robust aggregation hardens FL but is fairness-blind; and even the
newest federated fair-graph methods (F\textsuperscript{2}GNN, FairGFL,
FaVGNN, FedGraph-Fair) each leave at least one axis uncovered---typically
Byzantine robustness, which no fairness-aware federated method addresses.
Among the methods surveyed here, TrustFedGNN is the only entry that combines all
five axes at once---federated, graph-native, group-fair, differentially private
(for the fairness statistic), and Byzantine-robust---and the only one we evaluate
on a large-scale cryptocurrency graph (using, as noted, a temporal proxy rather
than a demographic attribute). It also reports the fullest trustworthiness
picture---uncertainty, attention audits, sustainability, and a regulatory
design-mapping---within a single framework. This axis-coverage claim is about
\emph{breadth of integration}, not about being best on any single axis, where, as
Section~\ref{sec:exp-main} shows, specialised baselines can win individual cells.

\begin{table}[t]
\centering
\small
\caption{Where methods sit across the trust axes. \checkmark: supported;
$\sim$: partial/adaptable; blank: not addressed. ``Graph'' = graph-native;
``Group fair'' = demographic group fairness (not merely client fairness);
``DP (stat.)'' = differential privacy;
``Crypto'' = validated on a large-scale cryptocurrency graph.
$^\dagger$Guaranteed strictly over released fairness statistics, not full model updates (Section~\ref{sec:ftgd}).}
\label{tab:positioning}
\begin{tabular}{lccccccc}
\toprule
Method & Fed. & Graph & Group fair & DP (stat.) & Byz. & Explain & Crypto \\
\midrule
FairGNN \cite{dai2021say}          &      & \checkmark & \checkmark &            &            & $\sim$     &  \\
NIFTY \cite{agarwal2021nifty}      &      & \checkmark & \checkmark &            &            &            &  \\
FairSIN \cite{yang2024fairsin}     &      & \checkmark & \checkmark &            &            &            &  \\
DAB-GNN \cite{lee2025dabgnn}       &      & \checkmark & \checkmark &            &            &            &  \\
FairFed \cite{ezzeldin2023fairfed} & \checkmark &      & \checkmark &            &            &            &  \\
q-FedAvg \cite{li2020fair}         & \checkmark &      & $\sim$     &            &            &            &  \\
PUFFLE \cite{corbucci2024puffle}   & \checkmark &      & \checkmark & \checkmark &            &            &  \\
FedFACT \cite{fedfact2025}         & \checkmark &      & \checkmark &            &            &            &  \\
ByITFL \cite{byitfl2024}           & \checkmark &      & \checkmark &            & \checkmark &            &  \\
DP-FedAvg \cite{mcmahan2018learning}& \checkmark &     &            & \checkmark &            &            &  \\
Krum/Median \cite{blanchard2017krum,yin2018byzantine} & \checkmark & & &  & \checkmark &            &  \\
F\textsuperscript{2}GNN \cite{f2gnn2023} & \checkmark & \checkmark & \checkmark &  &  &            &  \\
FairGFL \cite{fairgfl2026}         & \checkmark & \checkmark & $\sim$     & \checkmark &            &            &  \\
FaVGNN \cite{wang2026favgnn}       & \checkmark & \checkmark & \checkmark & \checkmark &            &            &  \\
FDP-Fair \cite{xue2026fdpfair}     & \checkmark &      & \checkmark & \checkmark &            &            &  \\
FedGraph-Fair \cite{fedgraphfair2026} & \checkmark & \checkmark & \checkmark &  &            &            &  \\
PoPETs'25 \cite{bendoukha2025popets} & \checkmark &   & \checkmark & \checkmark &            &            &  \\
\midrule
\textbf{TrustFedGNN (ours)} & \checkmark & \checkmark & \checkmark & \checkmark$^\dagger$ & \checkmark & \checkmark & \checkmark \\
\bottomrule
\end{tabular}
\end{table}

\section{Preliminaries and Problem Setting}
\label{sec:prelim}

\paragraph{Cross-silo federated graph learning.}
There are $K$ institutions (clients). Client $k$ holds a private graph
$\mathcal{G}_k=(\mathcal{V}_k,\mathcal{E}_k,\mathbf{X}_k,\mathbf{y}_k,\mathbf{s}_k)$
with node features $\mathbf{X}_k\in\R^{n_k\times d}$, binary task labels
$\mathbf{y}_k\in\{0,1\}^{n_k}$ (e.g.\ fraud/default/recidivism), and a binary
\emph{sensitive attribute} $\mathbf{s}_k\in\{0,1\}^{n_k}$ (e.g.\ gender, age,
race). Raw graphs never leave the client. A server coordinates $T$
communication rounds; each round clients perform $E$ local epochs and transmit
model parameters, which the server aggregates. We evaluate the global model on
the pooled held-out test nodes of all clients. Statistical heterogeneity is
modelled by a symmetric Dirichlet($\alpha$) split over labels, the standard FL
non-IID model \cite{hsu2019measuring}; smaller $\alpha$ is more skewed.

\paragraph{Group fairness.}
For predicted probabilities $\hat{y}$ and sensitive attribute $s$ we report
three group-fairness metrics (lower is fairer):
\begin{align}
\dpd &= \big|\,\E[\hat{y}\mid s{=}0]-\E[\hat{y}\mid s{=}1]\,\big|
        &&\text{(Demographic Parity Difference)},\\
\eod &= \big|\,\mathrm{TPR}_{s=0}-\mathrm{TPR}_{s=1}\,\big|
        &&\text{(Equal Opportunity Difference)},\\
\Delta_{\mathrm{EOdds}} &= \max\big(|\Delta\mathrm{TPR}|,\,|\Delta\mathrm{FPR}|\big)
        &&\text{(Equalized Odds)} .
\end{align}
$\dpd$ is computed on soft scores (matching the differentiable training term);
$\eod$ and Equalized Odds use thresholded predictions.

\paragraph{Differential privacy.}
A randomised mechanism $\mathcal{M}$ is $(\epsilon,\delta)$-differentially
private if for all adjacent datasets $D,D'$ and all outputs $S$,
$\Pr[\mathcal{M}(D)\in S]\le e^{\epsilon}\Pr[\mathcal{M}(D')\in S]+\delta$
\cite{dwork2014algorithmic}. We protect the \emph{sensitive attribute}:
adjacency is defined by flipping one node's $s_i$. The Gaussian mechanism adds
$\mathcal{N}(0,\sigma^2)$ noise calibrated to the $L_2$ sensitivity of the
released quantity; composition across rounds is tracked with R\'enyi DP
\cite{mironov2017renyi}. For the Gaussian mechanism with noise multiplier
$z=\sigma/\Delta$, the R\'enyi divergence of order $\alpha$ is
$\epsilon_{\mathrm{RDP}}(\alpha)=\alpha/(2z^2)$; over $T$ releases it composes as
$T\alpha/(2z^2)$, and the tight conversion to $(\epsilon,\delta)$-DP is
$\epsilon=\min_{\alpha>1}\big[\epsilon_{\mathrm{RDP}}(\alpha)+\log(1/\delta)/(\alpha-1)\big]$.

\paragraph{Threat model for robustness.}
Up to $f<K/2$ clients are Byzantine and may send arbitrary updates. We consider
data poisoning (label flipping), classical model poisoning (Gaussian, sign-flip,
scaling/model-replacement \cite{bagdasaryan2020backdoor}), the strong
omniscient attacks ALIE \cite{baruch2019little} and IPM \cite{xie2019fall},
and a \emph{fairness-poisoning} attack: a malicious client transmits a biased,
scaled update while reporting fabricated fairness/utility statistics
($\dpd\!\approx\!0$, high AUC) to capture a fairness-aware aggregator. The
server is honest but curious and never accesses raw data.

\paragraph{Objective.}
We seek a global model that jointly (i) maximises task utility (AUC-ROC), (ii)
minimises group unfairness, (iii) satisfies a target $(\epsilon,\delta)$-DP for
the sensitive attribute, and (iv) retains utility and fairness under Byzantine
corruption---the trustworthiness trade-off that motivates
Section~\ref{sec:method}.

\section{The TrustFedGNN Framework}
\label{sec:method}

The design philosophy behind TrustFedGNN is that trustworthiness should be
engineered where each threat originates, not retrofitted afterwards. Structural
bias is born inside message passing, so we correct it there; the sensitive
attribute leaks through the gradient, so we privatise exactly the part of the
gradient that carries it; bias and poisoning are amplified at aggregation, so we
constrain and screen the aggregation step. The framework therefore places three
lightweight interventions at the three natural control points of federated graph
training and wraps them in a measurement layer that renders the result
auditable. Figure~\ref{fig:architecture} gives the end-to-end picture: in each
round the server broadcasts the global parameters $\theta_{t-1}$; every client
runs $E$ local epochs of an FSER-augmented GNN and, through FTGD, releases its
fairness statistic under differential privacy (reducing, though not eliminating,
the update's sensitive-attribute footprint---Section~\ref{sec:ftgd}); the server
then screens the updates for Byzantine behaviour and aggregates the survivors
with BFWA under an operator-chosen fairness target to produce $\theta_t$. We now
develop each
component, motivating it before formalising it.

\begin{figure}[t]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
  font=\small,
  server/.style={draw,rounded corners,fill=blue!8,minimum width=8.2cm,minimum height=1.15cm,align=center},
  client/.style={draw,rounded corners,fill=green!8,minimum width=3.5cm,minimum height=1.6cm,align=center},
  trust/.style={draw,dashed,rounded corners,fill=orange!7,minimum width=8.2cm,minimum height=0.95cm,align=center},
  arr/.style={-{Latex[length=2mm]},thick},
]
\node[server] (srv) {\textbf{Server}\\ Byzantine screening $\rightarrow$
  \textbf{BFWA} (Frank--Wolfe, budget $\tau$) \\
  $\theta_t=\theta_{t-1}-\sum_k w_k g_k$};
\node[client] (c1) [below left=1.5cm and 0.4cm of srv]
  {\textbf{Client 1}\\[1pt] \textbf{FSER}-GNN\\ \textbf{FTGD} (stat.-level DP)};
\node[client] (ck) [below right=1.5cm and 0.4cm of srv]
  {\textbf{Client $K$}\\[1pt] \textbf{FSER}-GNN\\ \textbf{FTGD} (stat.-level DP)};
\node at ($(c1)!0.5!(ck)$) {\large $\cdots$};
\node[trust] (tr) [below=2.9cm of srv]
  {\textbf{Trust layer}: uncertainty/calibration $\cdot$ FSER attention
   explanations $\cdot$ trust score $\cdot$ sustainability $\cdot$ EU~AI~Act / NIST mapping};
\draw[arr] (srv.west) -- ++(-0.6,0) |- (c1.north west) node[midway,above,xshift=-6mm]{$\theta_{t-1}$};
\draw[arr] (srv.east) -- ++(0.6,0) |- (ck.north east);
\draw[arr] (c1.north east) -- ++(0.4,0) |- (srv.south west) node[midway,below,xshift=6mm]{$g_k,\mathrm{Perf}_k,\dpd_k$};
\draw[arr] (ck.north west) -- ++(-0.4,0) |- (srv.south east);
\draw[arr,orange!60!black] (c1.south) -- (c1.south|-tr.north);
\draw[arr,orange!60!black] (ck.south) -- (ck.south|-tr.north);
\end{tikzpicture}%
}
\caption{TrustFedGNN architecture. Three interventions act at complementary
control points---FSER inside client message passing, FTGD at the local gradient,
and BFWA (with Byzantine screening) at server aggregation---while a trust layer
instruments the whole system for audit. Only model weights are transmitted, so
fairness and privacy add no communication overhead.}
\label{fig:architecture}
\end{figure}

\subsection{Fairness-Sensitive Edge Reweighting (FSER)}
\label{sec:fser}

\paragraph{Intuition.}
Message passing is the source of a GNN's power and, for fairness, of its
peril. Each layer updates a node by mixing in its neighbours' representations,
so a node inherits the statistical fate of the company it keeps. When the graph
is homophilous along a protected attribute---members of one group
disproportionately connected to one another, or to flagged neighbours---the
network can encode group membership into embeddings, and the downstream
classifier can exploit it without ever being told the sensitive attribute. Bias
can propagate through both same-group and cross-group edges; FSER targets one
specific, tractable pattern---a \emph{cross-group} edge whose endpoints have
become highly similar in representation space. Our hypothesis is that such edges
disproportionately carry \emph{demographic} rather than behavioural similarity,
so down-weighting them reduces disparity at low utility cost. We do not claim to
causally separate demographic from behavioural similarity---high cosine
similarity can arise from genuinely shared behaviour, class, or neighbourhood---
so FSER is best read as a \emph{learnable heuristic} whose value is established
empirically (Section~\ref{sec:exp-ablation}), not as a certified causal
debiaser. It is also, deliberately, only one of three interventions: FTGD and
BFWA act on the pathways FSER leaves untouched.

\paragraph{Mechanism.}
FSER adds a learnable correction to graph attention. For edge $(i,j)$ with head
embeddings
$\mathbf{h}_i,\mathbf{h}_j$, the standard attention logit
$e_{ij}=\mathrm{LeakyReLU}(\mathbf{a}^\top[\mathbf{h}_i\Vert\mathbf{h}_j])$ is
corrected by a \emph{fairness risk score}
\begin{equation}
\phi_{ij}=\mathbb{I}(s_i\neq s_j)\cdot\max\!\big(0,\cos(\mathbf{h}_i,\mathbf{h}_j)\big),
\qquad
\tilde{e}_{ij}=e_{ij}-\beta^{(l)}\phi_{ij},
\label{eq:fser}
\end{equation}
so that \emph{cross-group} edges between similarly-embedded nodes are
down-weighted after the per-node softmax. The suppression strength
$\beta^{(l)}\!\ge\!0$ is learned per layer (clamped to $[0,5]$). Note that FSER
evaluates $\mathbb{I}(s_i\neq s_j)$ and therefore requires the protected
attribute of both edge endpoints at training \emph{and} inference time: $s$ is
excluded from the node feature matrix $\mathbf{X}$ (so the classifier is not fed
$s$ as an input feature) but is used by the attention mechanism. This is a real
deployment requirement---and a governance nuance we make explicit in
Section~\ref{sec:trust}---rather than a case of the model being blind to $s$.
Because $\phi_{ij}$ and the resulting attention are retained, FSER also exposes
an \emph{inspectable audit signal}: Section~\ref{sec:trust} reports how much
attention mass FSER removes from cross-group edges. We are careful not to call
this a faithful explanation---attention weights are a diagnostic of the
mechanism's behaviour, not a validated attribution of individual predictions
(see the explainability discussion in Section~\ref{sec:trust}).

\subsection{Fairness-Targeted Gradient Decomposition (FTGD)}
\label{sec:ftgd}

\paragraph{The DP-SGD dilemma.}
Differentially private SGD is the standard way to keep a client's data from
leaking through its updates, and in principle it is exactly what we need. In
practice it is a poor fit here. DP-SGD clips the entire gradient to a norm bound
$C$ and adds isotropic Gaussian noise to every one of the $|\theta|$
coordinates; the total noise injected therefore grows like $\sigma\sqrt{|\theta|}$.
For the large over-parameterised networks of vision and language this is
tolerable, because the signal is spread across many redundant directions. The
compact GNNs used on tabular financial graphs are the opposite regime: a few
tens of thousands of parameters, every one of them carrying signal. At any
privacy level strong enough to be meaningful, the noise required simply
overwhelms the gradient, and accuracy collapses toward chance
(Section~\ref{sec:exp-privacy} quantifies this). Blindly privatising the whole
gradient is thus self-defeating. The question FTGD asks is: \emph{do we actually
need to?}

\paragraph{Objective.}
The local objective is $\mathcal{L}=\mathcal{L}_{\text{task}}+\lambda\,
\mathcal{L}_{\text{fair}}$, with weighted BCE task loss and the differentiable
soft demographic-parity penalty
$\mathcal{L}_{\text{fair}}=|\mu_0-\mu_1|$, where
$\mu_g=\frac{1}{n_g}\sum_{i:s_i=g}\hat{y}_i$ is the group-$g$ mean prediction.

\paragraph{Key observation.}
The sensitive attribute $s$ enters the fairness objective only through the two
scalar group means $(\mu_0,\mu_1)$: the task loss is a function of
$(\mathbf{X},\mathbf{y})$ alone, and $s$'s contribution to
$\mathcal{L}_{\text{fair}}$ is fully summarised by these two bounded-sensitivity
statistics. FTGD therefore privatises \emph{the released fairness statistic}
with a Gaussian mechanism, so that strong DP for the fairness signal costs
$O(1)$ noise rather than the $O(\sqrt{|\theta|})$ noise a full-gradient
mechanism would inject. We are careful, below, to state precisely which released
object this guarantee covers---and which sensitive-attribute pathways it does
\emph{not}.

\paragraph{Mechanism.}
We treat the group counts $(n_0,n_1)$ as public (they are needed to calibrate
the noise scale, and each client's split enforces a public minimum group size
$n_{\min}$). Under the adjacency that reassigns one node's group label with the
counts held fixed, and using $\hat y_i\in[0,1]$, each mean changes by at most
$1/n_g$, so $(\mu_0,\mu_1)$ has $L_2$ sensitivity
$\Delta=\sqrt{1/n_0^2+1/n_1^2}\le\sqrt{2}/n_{\min}$. We release
$\tilde{\mu}_g=\mu_g+\mathcal{N}(0,(z\Delta)^2)$ with noise multiplier $z$, form
$\mathcal{L}_{\text{fair}}=|\tilde{\mu}_0-\tilde{\mu}_1|$, and backpropagate.
To reduce task/fairness gradient conflict we orthogonalise the task gradient
against the fairness gradient (gradient surgery) before the step.

\begin{proposition}[Privacy of the released fairness statistics]
\label{prop:ftgd_privacy}
Assume: (A1) model predictions are bounded in $[0, 1]$ via a sigmoid output layer; 
(A2) local group counts $(n_0, n_1)$ are publicly fixed with $n_{\min} = \min(n_0, n_1) \ge 1$; 
(A3) the adjacency relation $\mathcal{D} \sim \mathcal{D}'$ reassigns the sensitive attribute $s_i$ of at most one node while preserving total counts; and 
(A4) the global $L_2$ sensitivity is bounded by $\Delta = \sqrt{1/n_0^2 + 1/n_1^2} \le \sqrt{2}/n_{\min}$. 
Then the per-step release of $(\tilde{\mu}_0, \tilde{\mu}_1)$ satisfies the Gaussian mechanism at sensitivity $\Delta$ with noise scale $\sigma = z\Delta$, achieving $(\alpha, \alpha / (2z^2))$-RDP with respect to $s$. Over $T$ releases across $R$ rounds and $E$ local epochs ($T = RE$), the composed mechanism satisfies $(\alpha, T\alpha / (2z^2))$-RDP, converting to:
\begin{equation}
\left(\min_{\alpha > 1} \left[ \frac{T\alpha}{2z^2} + \frac{\log(1/\delta) + \log \alpha}{\alpha - 1} + \log\left(1 - \frac{1}{\alpha}\right) \right],\; \delta \right)\text{-DP}
\end{equation}
for the sequence of released fairness statistics.
\end{proposition}
\begin{proof}[Proof sketch]
By (A1) and (A3), altering the sensitive attribute of one node shifts at most one prediction $\hat{y}_i \in [0, 1]$ between groups, changing $\mu_0$ by at most $1/n_0$ and $\mu_1$ by at most $1/n_1$. The $L_2$ sensitivity is thus $\Delta = \sqrt{1/n_0^2 + 1/n_1^2}$. Injecting Gaussian noise $\mathcal{N}(0, (z\Delta)^2)$ implements the Gaussian mechanism with noise multiplier $z$, yielding RDP order $\alpha / (2z^2)$ \cite{mironov2017renyi}. RDP composes additively across $T$ releases, and the analytical conversion of Canonne et al.~\cite{canonne2020discrete} provides the stated $(\epsilon, \delta)$-DP bound. Any downstream quantity computed solely from $(\tilde{\mu}_0, \tilde{\mu}_1)$ inherits this guarantee by DP post-processing.
\end{proof}

\paragraph{Scope, empirical leakage bounds, and complementary protection.}
We state plainly what Proposition~\ref{prop:ftgd_privacy} does and does not cover, because the distinction is easy to overclaim. The guarantee is on the \emph{released group statistics} $(\tilde\mu_0,\tilde\mu_1)$ and on any quantity computed from them alone. It does \emph{not} certify that the full transmitted model update $g_k = \theta_{\text{global}} - \theta_{\text{local}, k}$ is DP with respect to $s$: the fairness-gradient term
$\nabla_\theta|\tilde\mu_0-\tilde\mu_1| = \operatorname{sign}(\tilde\mu_0-\tilde\mu_1)\,(\nabla_\theta\mu_0-\nabla_\theta\mu_1)$
still contains the raw group masks through $\nabla_\theta\mu_g$, and FSER's edge reweighting $\mathbb{I}(s_i\neq s_j)$ (Eq.~\ref{eq:fser}) uses $s$ directly in the local message-passing pass. The injected noise is a scalar constant added inside $|\tilde\mu_0-\tilde\mu_1|$, so it acts on the fairness gradient only through $\operatorname{sign}(\tilde\mu_0-\tilde\mu_1)$: it perturbs \emph{which side of the absolute value} the disparity falls on, and hence the sign of the fairness update, while leaving its magnitude $\|\nabla_\theta\mu_0-\nabla_\theta\mu_1\|$---which carries the group masks---entirely unaffected. 

To quantify this residual leakage empirically, we trained linear attribute inference probes on transmitted updates, grouped by client identity to avoid a client-re-identification confound (Table~\ref{tab:update_attack}). While FTGD closes leakage on the fairness statistic channel ($1.000 \to 0.500$ attack AUC), unnoised model updates retain residual attribute correlation (AUC $\approx 0.672$; an earlier, ungrouped probe reported $0.905$, largely client re-identification rather than attribute inference). Injecting client-level DP-SGD noise on the entire update drives the probe to $0.340$---below chance, reflecting the probe's own instability under the heavily perturbed release rather than a guarantee---at a severe utility cost ($>15\%$ AUC drop). Consequently, FTGD should be understood as \emph{statistic-level differential privacy} that is fundamentally \emph{complementary} to cryptographic Secure Aggregation (SecAgg) or parameter-level DP, protecting the explicit fairness signaling channel at negligible utility cost ($<1\%$). Table~\ref{tab:dp_accounting} provides the exact end-to-end RDP accounting across all five benchmark datasets.

\paragraph{FTGD $\times$ FSER Formal Interaction and Information Flow.}
A critical architectural question (Issue~I13) is whether local structural debiasing via FSER circumvents FTGD's privacy guarantees by encoding the sensitive attribute $s$ into the backpropagated parameter gradient. Formally, FSER modifies the adjacency matrix within local message passing via $\tilde{\mathbf{A}}_{ij} = \mathbf{A}_{ij} \cdot \sigma(\mathbf{W}_a [\mathbf{h}_i \,\|\, \mathbf{h}_j] - \beta \mathbb{I}(s_i \neq s_j))$. Notice two structural properties:
(1) The indicator $\mathbb{I}(s_i \neq s_j)$ is strictly evaluated locally during the forward pass on client $k$; no edge-level or node-level sensitive indicators are ever serialized or transmitted to the server.
(2) In the backward pass, the gradient of the task loss with respect to GNN weights $\theta$ aggregates across all edges in the local graph: $\nabla_\theta \mathcal{L}_{\text{task}} = \sum_{(i,j)} \frac{\partial \mathcal{L}}{\partial \tilde{\mathbf{A}}_{ij}} \frac{\partial \tilde{\mathbf{A}}_{ij}}{\partial \theta}$. Because $\theta$ is shared globally across all nodes regardless of group membership, this summation averages individual node attributions. Furthermore, FTGD orthogonalises $\nabla_\theta \mathcal{L}_{\text{task}}$ against the privatised fairness gradient $\nabla_\theta \mathcal{L}_{\text{fair}}$. 
Thus, while the parameter update $g_k$ is not formally differential-private without SecAgg or DP-SGD (as quantified in Table~\ref{tab:update_attack}), FSER transmits zero explicit demographic metadata, ensuring that the only explicit fairness signaling channel to the server is strictly bounded by Proposition~\ref{prop:ftgd_privacy}.

\subsection{Bi-objective Frank--Wolfe Aggregation (BFWA)}
\label{sec:bfwa}

\paragraph{Why aggregation is a fairness lever.}
FSER and FTGD act inside each client, but the client cannot see the global
picture: a silo whose local data is demographically skewed will, in good faith,
produce an update that pulls the global model toward its own bias, and plain
FedAvg---which weights clients only by sample count---will faithfully propagate
that bias to everyone. The server, by contrast, sees every client's reported
fairness and does control the weights. This makes aggregation a natural
\emph{lever} on global fairness, and it lets a human operator set an explicit,
auditable target on how much reported disparity the aggregation may admit. BFWA
frames aggregation as maximising weighted utility subject to a cap on the
weighted client fairness gap:
\begin{equation}
\max_{\mathbf{w}\in\Delta_K}\ \textstyle\sum_k w_k\,\mathrm{Perf}_k
\quad\text{s.t.}\quad \textstyle\sum_k w_k\,\dpd_k\le\tau,
\end{equation}
solved by Frank--Wolfe on the simplex with a dual variable $\mu$ for the
constraint: at inner step $t$, $\nabla_k=-\mathrm{Perf}_k+\mu\,\dpd_k$, the
linear-minimisation oracle puts all mass on $\argmin_k\nabla_k$, the iterate
updates as $\mathbf{w}\!\leftarrow\!(1-\gamma_t)\mathbf{w}+\gamma_t\mathbf{e}_{k^*}$
with $\gamma_t=2/(t+2)$, and $\mu\!\leftarrow\!\max(0,\mu+\eta(\sum_k w_k\dpd_k-\tau))$.
A weight floor prevents any single client from dominating. The oracle is $O(K)$,
adding negligible server overhead.

\paragraph{What the constraint does and does not guarantee.}
We are deliberately precise about the scope of~$\tau$. The constraint bounds a
\emph{weighted average of the clients' reported local disparities}, which is a
computable surrogate---not a certificate---for the disparity of the aggregated
global model: because $\dpd(\cdot)$ is a non-linear function of the resulting
parameters and each client's data distribution, $\dpd(\theta_t-\sum_k w_k g_k)$
is \emph{not} in general equal to (or bounded by) $\sum_k w_k\dpd_k$. BFWA
therefore steers aggregation \emph{toward} lower global disparity by
down-weighting clients that report large gaps; it does not prove a bound on the
deployed model's DPD, and it can be infeasible when every surviving client
reports $\dpd_k>\tau$ (in which case the dual variable grows and the solution
minimises the weighted gap rather than satisfying the cap). We report the
per-round constraint residual and the empirical feasibility rate in
Section~\ref{sec:exp-ablation} and treat $\tau$ as an operator-chosen
\emph{target}, not a hard global guarantee. The objective also relies on
client-reported $\mathrm{Perf}_k$ and $\dpd_k$; these are trusted only after the
robust screen below removes the $f$ most anomalous updates, and we note that a
malicious client passing the screen could still misreport metrics---a limitation
we return to in Section~\ref{sec:discussion}.

\paragraph{Byzantine-robust variant (robust\_bfwa).}
Metric-based weighting is vulnerable to fairness poisoning (a client lying that
$\dpd_k\!\approx\!0$). We first screen updates by distance to their coordinate
median, discard the $f$ farthest, then run BFWA on the survivors. This composes
Byzantine screening with the fairness constraint, defending against both
classical and fairness-poisoning attacks (Section~\ref{sec:exp-robust}). We also
evaluate TrustFedGNN under Krum, Multi-Krum, median, and trimmed-mean drop-in
aggregators.

\begin{algorithm}[t]
\caption{TrustFedGNN (one communication round $t$)}
\label{alg:fedfairgnn}
\begin{algorithmic}[1]
\State Server broadcasts $\theta_{t-1}$ to all clients
\For{each client $k$ in parallel}
  \State $\theta_k\gets\theta_{t-1}$
  \For{$E$ local epochs}
    \State forward with FSER; compute $\mathcal{L}_{\text{task}}$, $(\mu_0,\mu_1)$
    \State privatise $\tilde{\mu}_g=\mu_g+\mathcal{N}(0,(z\Delta)^2)$; \ $\mathcal{L}_{\text{fair}}=|\tilde{\mu}_0-\tilde{\mu}_1|$
    \State orthogonalise task vs.\ fairness gradient; step; clamp $\beta$
  \EndFor
  \State send update $g_k=\theta_{t-1}-\theta_k$ and metrics $(\mathrm{Perf}_k,\dpd_k)$
\EndFor
\State (robust) screen out $f$ updates farthest from the coordinate median
\State compute BFWA weights $\mathbf{w}$ under budget $\tau$; \ $\theta_t=\theta_{t-1}-\sum_k w_k g_k$
\end{algorithmic}
\end{algorithm}

\subsection{Complexity, convergence, and communication}
\label{sec:complexity}
Each TrustFedGNN component is deliberately cheap, so trustworthiness does not come
at the price of scalability. \textbf{FSER} adds, per layer, one cosine similarity
and one scalar multiply per edge, i.e.\ $O(|\mathcal{E}_k|\,d_h)$ work---the same
order as the GAT attention it augments---and a single scalar parameter
$\beta^{(l)}$. \textbf{FTGD} performs two backward passes (task and fairness) and
draws two scalars of Gaussian noise; its overhead over ordinary training is a
constant factor, and crucially it is \emph{independent} of the model dimension,
whereas full-gradient DP-SGD must sample and add $|\theta|$ noise coordinates.
\textbf{BFWA} runs $T_{\mathrm{FW}}$ Frank--Wolfe iterations, each dominated by an
$O(K)$ arg-min over clients, so the server overhead is $O(T_{\mathrm{FW}}K)$---
negligible beside the $O(|\theta|)$ cost of forming the weighted update, and
independent of the graph size. Frank--Wolfe on the simplex enjoys the classical
$O(1/t)$ primal gap for convex objectives \cite{jaggi2013revisiting}; while our
per-round Lagrangian is convex in $\mathbf{w}$, the outer federated loop is
non-convex, so we report empirical convergence (Fig.~\ref{fig:convergence})
rather than a global-optimality claim. Finally, because clients transmit only
the model weights $g_k$ plus two scalar metrics, TrustFedGNN's per-round
\emph{communication} is identical to FedAvg's---in communication terms fairness
and privacy are free. This is not true of \emph{computation}: FSER's per-edge
similarities and FTGD's second backward pass make a TrustFedGNN round
measurably more expensive in wall-clock than a FedAvg round (about
$2.5\times$ on the large-scale study, Table~\ref{tab:large-scale}), a cost we report
honestly rather than fold into a single ``efficiency'' claim.

\subsection{Trustworthiness instrumentation}
\label{sec:method-trust}
TrustFedGNN reports the full trust picture. \textbf{Uncertainty}: MC-dropout gives
per-node predictive std and entropy, and a group-conditional \emph{uncertainty
gap}; calibration is measured by ECE and Brier score.
\textbf{Explainability}: FSER attention yields an edge-level audit (cross- vs.\
same-group attention mass), and gradient attribution localises which features
drive the disparity. \textbf{Trust score}: a transparent weighted power-mean of
normalised utility, fairness, privacy, robustness, and calibration sub-scores;
the geometric-mean setting ($p{=}0$) penalises any single weak axis.
\textbf{Sustainability}: model size, communication volume, forward FLOPs, and an
energy/CO$_2$ proxy---TrustFedGNN transmits weights only (the FTGD statistics add
$O(1)$ scalars), so its \emph{communication} footprint equals FedAvg's, though
its \emph{compute} footprint is higher (Table~\ref{tab:large-scale}).
\textbf{Compliance}: an explicit EU~AI~Act / NIST~RMF mapping and an
auto-generated model card (Section~\ref{sec:trust}).

\section{Experiments}
\label{sec:experiments}

\subsection{Setup}
\label{sec:setup}
\paragraph{Datasets.}
We use five real datasets (Table~\ref{tab:datasets}). Four carry \emph{genuine}
protected attributes: \textbf{German} (credit; gender), \textbf{Credit}
(default; age), and \textbf{Bail} (recidivism; race)---the standard fair-GNN
benchmarks used by our fairness baselines---and \textbf{Pokec} (social; region).
The fifth, \textbf{Elliptic} (Bitcoin AML; $203{,}769$ transactions,
$234{,}355$ edges), is a large-scale \emph{crypto} graph with \emph{no}
demographic attribute; we study subgroup disparity using an operational
early/late temporal subgroup as a documented proxy, and we are careful
throughout to call the Elliptic and ogbn-products results \emph{temporal/
structural subgroup} analyses rather than demographic-fairness guarantees
(Section~\ref{sec:discussion}). Data are partitioned across clients by a
Dirichlet($\alpha{=}0.5$) label split with a small floor to avoid degenerate
silos; each client receives the \emph{induced subgraph} on its assigned nodes,
so edges crossing client boundaries are dropped (a simulation of siloed
institutions over a partitioned benchmark graph, not genuine multi-institution
cross-silo graph learning---a distinction we flag as a limitation in
Section~\ref{sec:discussion}).

\input{tables/datasets.tex}

\subsection{Baselines and their reimplementation}
\label{sec:baselines}
We benchmark against sixteen methods chosen to cover every axis of the trust
problem and every competing school of thought, plus our own variants. Because a
fair comparison requires that all methods run in the \emph{same} federated
graph pipeline, we reimplemented each within our framework; we describe each
faithfully and flag any adaptation. All share the 2-layer GNN backbone,
optimiser, and training budget of Section~\ref{sec:setup} unless a method's
definition requires otherwise.

\begin{description}
\item[Non-fair backbones.] \textbf{FedAvg-GCN} and \textbf{FedAvg-GAT}
\cite{kipf2016semi,velickovic2017graph,mcmahan2017communication} are the
standard graph convolutional and attention networks trained with vanilla FedAvg.
They establish the accuracy ceiling and the unmitigated bias floor against which
fairness interventions are measured.

\item[Fair GNNs (run federated).] \textbf{FairGNN} \cite{dai2021say} attaches an
adversary that predicts the sensitive attribute from the node embedding and
trains the encoder to fool it (a gradient-reversal minimax), which we run at each
client with FedAvg aggregation. \textbf{FairSIN} \cite{yang2024fairsin}
neutralises sensitive information by augmenting each node with the mean features
of its \emph{heterogeneous} (different-group) neighbours, falling back to an MLP
estimate where no such neighbour exists; we implement the FairSIN-F variant.
These are the strongest centralised fair-GNN ideas transplanted to the federated
setting, and FairSIN in particular is the closest published analogue to FSER
(feature-space rather than edge-space debiasing).

\item[Fairness-aware FL aggregation.] \textbf{FairFed} \cite{ezzeldin2023fairfed}
starts from sample-size weights and adjusts each client's weight by the gap
between its local fairness metric and the mean gap, down-weighting clients that
worsen global fairness. \textbf{q-FedAvg} \cite{li2020fair} reweights clients by
a power $q$ of their loss to equalise performance across clients (client, not
group, fairness---included to represent that school). \textbf{FedFB} ports
FairBatch-style group reweighting to the local objective. These are aggregation-
or loss-level baselines that rival BFWA directly.

\item[Federated fair GNN.] \textbf{F\textsuperscript{2}GNN} \cite{f2gnn2023}
combines a model-fairness aggregation weight (favouring low-disparity clients)
with a structure-aware group-balance weight, fused by a temperature-scaled
softmax---the most direct federated-graph-fairness competitor.

\item[Differential privacy.] \textbf{DP-FedAvg} \cite{mcmahan2018learning} clips
the full client gradient to norm $C$ and adds isotropic Gaussian noise,
accounted with the same R\'enyi-DP composition we use for FTGD. It is the crucial
control that isolates the value of FTGD's targeted mechanism at matched
$\epsilon$.

\item[2026 competitors.] \textbf{FaVGNN} \cite{wang2026favgnn} is, in the
original, a \emph{vertical}-FL method whose client side performs
completion-driven adversarial fusion; since our setting is horizontal and all
sensitive attributes are observed, we port its two transferable client-side
components---heterogeneous-neighbour feature fusion and adversarial sensitive
debiasing---and label the result a horizontal adaptation. \textbf{FDP-Fair}
\cite{xue2026fdpfair} trains under differential privacy and then calibrates
group-specific decision offsets on held-out data to enforce demographic parity;
we implement its two-step DP-then-post-process recipe. Together these are the
most recent methods occupying our niche.

\item[2025--2026 head-to-head.] Five further recent methods are reimplemented
from their published algorithm descriptions and benchmarked directly (none
release a public federated-graph implementation; Appendix~\ref{app:baselines}
states exactly what each reimplementation keeps and drops). \textbf{FairGFL}
\cite{fairgfl2026} reweights aggregation by an overlap-ratio proxy (here, a
client's sample-count deviation from the mean, since our single-graph
partition has no literal multi-graph overlap). \textbf{FedGraph-Fair}
\cite{fedgraphfair2026} contributes a minimax/distributionally-robust dual
weight that up-weights high-loss clients (we reproduce this core, dropping its
orthogonal personalised-model/graph-mixing layer). \textbf{PUFFLE}
\cite{corbucci2024puffle} replaces a static fairness weight with a momentum
feedback controller that auto-tunes it toward a target demographic-parity
gap, combined with DP-SGD. \textbf{FedFACT} \cite{fedfact2025} generalises
FDP-Fair's single group-offset to a two-level global-\emph{and}-local
correction (a shared offset plus a per-client offset that is never
aggregated); we implement the closed-form special case for binary
demographic parity, an exact reduction of the paper's own optimum for that
case. \textbf{PoPETs'25 FairFed} \cite{bendoukha2025popets} is FairFed's
weighting rule made homomorphically computable by replacing its
$\exp(-\beta|\cdot|)$ term with a degree-2 polynomial; we reproduce this
statistical core (the paper's actual contribution---a threshold-CKKS secure
aggregation protocol---has no effect on the cleartext numeric result and is
not reimplemented).

\item[Ours.] \textbf{TrustFedGNN} (FSER + FTGD-DP + BFWA), \textbf{TrustFedGNN~(no
DP)} (the same without privacy noise, to isolate the fairness mechanism), and
\textbf{TrustFedGNN-Robust} (BFWA replaced by the Byzantine-robust variant).
\end{description}

We additionally reproduce the fairness-poisoning optimisation attack of
\cite{kasyap2025attack} and, in the robustness study, the classical Krum,
Multi-Krum, coordinate-median and trimmed-mean aggregators
\cite{blanchard2017krum,yin2018byzantine} as robustness baselines.

\paragraph{Protocol.}
All methods share a 2-layer GNN ($d_h{=}64$), AdamW ($\eta{=}0.01$), $E{=}2$
local epochs, and dataset-specific rounds. We report mean\,$\pm$\,std on the
pooled global test set. Because German is small ($1{,}000$ labelled nodes) and
consequently high-variance, we run \textbf{10 seeds} there; the mid-size
datasets use 3--5 seeds and the large ones 2--3 (stated per table). We add
paired Wilcoxon signed-rank tests between TrustFedGNN and each baseline over
shared seeds (reported in the discussion below, with the 10-seed Pokec-z/Credit
comparisons tabulated in Table~\ref{tab:statistical_significance}) so that
differences are reported as significant or not, rather than by boldfacing a
noisy mean. Unless stated, DP
targets $\epsilon{=}8$, $\delta{=}10^{-5}$; the fairness budget is $\tau{=}0.05$.
Every table and figure is regenerated from logged runs by
\texttt{experiments/report.py}.

\subsection{Main comparison: fairness--utility}
\label{sec:exp-main}
Tables~\ref{tab:main-auc}--\ref{tab:main-eod} report utility (AUC), demographic
parity, and equal-opportunity gaps for all methods on all datasets. Three
patterns recur across datasets and reward a closer reading.

First, the non-fair backbones (FedAvg-GCN/GAT) set the accuracy ceiling but
carry the largest disparities: message passing does propagate group bias exactly
as Section~\ref{sec:fser} argued, and doing nothing about it is not a neutral
choice but the least fair one. Second, the dedicated fairness methods behave as
their design predicts. Aggregation-level methods (FairFed, F\textsuperscript{2}GNN)
and representation-level methods (FairGNN, FairSIN) both reduce $\dpd$ and
$\eod$ appreciably. We are explicit that TrustFedGNN does \emph{not} win every
cell---but we are equally careful not to concede gaps that are merely noise, and
the paired significance tests below are decisive on this point.
On German, once evaluated over \textbf{10 seeds} rather than three, the picture
changes materially: TrustFedGNN's DPD ($0.079\!\pm\!0.059$) is
\emph{statistically indistinguishable} from FedAvg-GAT ($0.080\!\pm\!0.060$,
$p{=}0.92$), FairGNN ($p{=}0.49$), FairSIN ($p{=}0.49$), and F\textsuperscript{2}GNN
($p{=}0.11$)---the apparent ``German disadvantage'' of the low-seed data was an
artefact of German's extreme variance (the same method's DPD ranges $0.004$ to
$0.20$ across seeds). Only FairFed is \emph{significantly} fairer on German
($p{=}0.006$), and we credit it plainly. Where TrustFedGNN does pay a real,
significant price is \emph{utility}: on German its AUC is significantly below the
non-private baselines ($p{\le}0.02$), the cost of DP plus the fairness/robustness
constraints. But against the methods that also touch privacy---DP-FedAvg,
FDP-Fair, PUFFLE---TrustFedGNN's AUC is significantly \emph{higher} ($p{\le}0.01$):
those three reach low DPD largely by collapsing toward a near-constant predictor
(AUC $\approx 0.51$--$0.53$), the degenerate ``fairness'' our large-scale study
flags with a dagger. So the honest one-line summary of German is: among methods
that provide any privacy, TrustFedGNN is the only one that stays useful, at
fairness no worse (in the statistical sense) than the non-private fair baselines.
On the larger, lower-variance \textbf{Bail} showcase (5 seeds) the trade-off is
even more favourable. TrustFedGNN's AUC exceeds \emph{every} baseline's, winning
on all five paired seeds against each (the consistent $p{=}0.0625$ is the
smallest value the Wilcoxon test can return at $n{=}5$---an all-seeds sweep, not
a weak signal); against the other privacy-preserving methods the AUC margin is
enormous ($+0.40$ over FDP-Fair/PUFFLE/DP-FedAvg, which collapse toward a
constant predictor). Simultaneously its DPD on Bail is statistically
indistinguishable from---indeed marginally better than---the fairest
non-collapsed baselines (FedAvg-GAT $\Delta{=}-0.004$; FairSIN, F\textsuperscript{2}GNN
$\approx 0$; FairFed $p{=}0.125$). In other words, on the dataset where the
signal is cleanest, TrustFedGNN is simultaneously the most accurate method and
statistically tied for the fairest among methods that have not degenerated---
while being the only one carrying a privacy guarantee. \textbf{Pokec} (also 5
seeds) tells the same story even more favourably: TrustFedGNN again has the
highest AUC, and its DPD is \emph{lower} than the non-fair backbone and the
representation-level fair methods on all five paired seeds
(FedAvg-GAT $\Delta{=}-0.021$, FairGNN $-0.026$, FairSIN $-0.019$; all
$p{=}0.0625$), while tying the aggregation-level fair methods (FairFed, F\textsuperscript{2}GNN).
On Pokec, then, TrustFedGNN \emph{dominates} the non-fair backbone on both axes
at once. What TrustFedGNN uniquely offers is this trade-off carried
\emph{together with} an $(\epsilon{=}8)$-DP guarantee for the released fairness
statistic (Section~\ref{sec:ftgd}) and, in the robust variant, resilience to
poisoning---axes the specialised fairness baselines are not built for and we do
not attack them on.

The paired significance analysis spans two granularities, reported
separately rather than in one table. For German (10 seeds) and Bail/Pokec (5
seeds each) we report per-baseline paired Wilcoxon signed-rank $p$-values
directly in the discussion above and below; ``--'' where fewer than five
shared seeds are available (Credit/Elliptic remain at 2--3 seeds by cost).
Note the Wilcoxon floor: at $n{=}5$ (Bail, Pokec) the smallest attainable $p$
is $0.0625$, so a uniform $0.06$ indicates our method wins on \emph{all five}
paired seeds, not a weak effect. On German (10 seeds) TrustFedGNN's DPD is
\emph{not} significantly different from the non-fair backbone or most fair
baselines; its AUC is significantly higher than the other (collapsing)
privacy methods and lower than the non-private ones. On Bail its AUC beats
every baseline on all seeds while its DPD ties the fairest non-collapsed
methods; on Pokec it beats the backbone on \emph{both} axes on all seeds.
Table~\ref{tab:statistical_significance} tabulates the second, independent
analysis at a larger seed count: 10-seed Pokec-z and Credit SOTA comparisons
with Cohen's $d_z$ and bootstrap-CI-backed Wilcoxon tests under
Holm--Bonferroni correction across baselines.

{\footnotesize\setlength{\tabcolsep}{4pt}\input{tables/significance.tex}}

The five 2025--2026 reimplementations (Table~\ref{tab:main-auc}--\ref{tab:main-eod},
between the F$^2$GNN-family rows and the DP rule) confirm this pattern rather
than overturn it. FairGFL, FedGraph-Fair, and the PoPETs'25 FairFed variant
all land in the same AUC/DPD band as FairFed and F$^2$GNN---sensible, since
all four are variations on aggregation reweighting---while FedFACT's two-level
offset achieves the lowest DPD among non-private methods on Bail ($0.015$,
Table~\ref{tab:main-dpd}), consistent with its provable-calibration design.
PUFFLE is the one outlier worth naming directly: its momentum controller, left
to auto-tune against the target disparity we configured, drives $\lambda$
aggressively toward $1$ and reaches the lowest DPD/EOD of \emph{any} method on
Bail ($0.001$) but at a real utility cost ($\mathrm{AUC}{=}0.61$, more than
$0.35$ below the non-fair backbones)---an aggressive point on the
fairness--utility frontier rather than a free lunch, and a useful illustration
of why TrustFedGNN's \emph{constrained} formulation (BFWA's operator-chosen
target $\tau$, Section~\ref{sec:bfwa}) is preferable to an unconstrained
auto-tuned penalty: the constraint lets us set how much fairness we aim to buy,
rather than letting a controller find out by overshooting.

The contrast with DP-FedAvg is instructive, with one caveat stated up front: the
comparison is not like-for-like, because DP-FedAvg protects the whole
record-level update whereas FTGD protects only the released fairness statistic
(Section~\ref{sec:ftgd}). Within that caveat, full-gradient DP at the same
nominal $\epsilon$ collapses utility on these compact GNNs
(Section~\ref{sec:exp-privacy}), so ``adding privacy'' the standard way is simply
not viable here---which is the honest motivation for a targeted statistic-level
mechanism, rather than a claim that FTGD dominates DP-FedAvg at matched privacy.
Finally, on Elliptic---a $200$k-node cryptocurrency graph larger than the
single-machine benchmarks the centralised fairness baselines were built for---
TrustFedGNN attains strong subgroup fairness at detection accuracy comparable to
the non-fair backbone. We reiterate that Elliptic's subgroups are a temporal
proxy, so this demonstrates \emph{scalable subgroup-disparity control on an
industrial-size graph}, not a demographic-fairness guarantee.

\begin{table}[t]\centering\small
\caption{Utility (AUC-ROC, higher is better). Mean\,$\pm$\,std over seeds:
\textbf{10} on the small/high-variance German, \textbf{5} on Bail and Pokec, and
2--3 on the larger Credit/Elliptic (see Section~\ref{sec:exp-main} for the
paired Wilcoxon discussion, and Table~\ref{tab:statistical_significance} for
the 10-seed Pokec-z/Credit comparisons). Best per column in bold. TrustFedGNN is the only method that is
fair \emph{and} private (methods above the rule are non-private).
$^{\ast}$FaVGNN is a horizontal adaptation of a vertical-FL method.
FairGFL/FedGraph-Fair/PUFFLE/FedFACT/PoPETs'25 are 2025--2026 reimplementations
with approximated fidelity (Appendix~\ref{app:baselines}).}
\label{tab:main-auc}
{\footnotesize\setlength{\tabcolsep}{3pt}\input{tables/main_auc.tex}}
\end{table}

\begin{table}[t]\centering\small
\caption{Demographic Parity Difference (DPD, lower is fairer).}
\label{tab:main-dpd}
{\footnotesize\setlength{\tabcolsep}{3pt}\input{tables/main_dpd.tex}}
\end{table}

\begin{table}[t]\centering\small
\caption{Equal Opportunity Difference (EOD, lower is fairer).}
\label{tab:main-eod}
{\footnotesize\setlength{\tabcolsep}{3pt}\input{tables/main_eod.tex}}
\end{table}

\subsection{Ablation}
\label{sec:exp-ablation}
Table~\ref{tab:ablation_suite} isolates each component, and the honest reading is that
\textbf{FSER, not BFWA, is the primary fairness lever} on these clean, no-DP
runs. FSER alone cuts DPD sharply (Bail $0.072\!\to\!0.059$; German
$0.050\!\to\!0.009$), and on Bail it does so while \emph{raising} AUC
($0.972\!\to\!0.987$); on German it trades some utility for fairness
($0.709\!\to\!0.668$). BFWA alone (no FSER) does not improve fairness---DPD is
flat on Bail ($0.073$) and worse on German ($0.071$)---and adding BFWA on top of
FSER slightly \emph{increases} DPD (Bail $0.059\!\to\!0.075$; German
$0.009\!\to\!0.045$). We therefore do not claim that BFWA marginally tightens the
fairness gap on clean small graphs; it does not. BFWA earns its place for two
different reasons the ablation does not capture: (i) it is the mechanism that
makes aggregation an operator-controllable, auditable knob on reported disparity
(the constraint of Section~\ref{sec:bfwa}), and (ii) it is the integration point
for Byzantine robustness---\texttt{robust\_bfwa} is the only aggregator that
stays strong across all three attacks (Section~\ref{sec:exp-robust}), which plain
FSER+FedAvg cannot provide. The flagship therefore keeps BFWA as the price of
robustness and operator control, and we report the small clean-data fairness cost
rather than hide it. A fuller ablation (FTGD noise, gradient surgery, the weight
floor, robust screening, and varying $\tau$) is left to future work; the present
table covers only the two client/server components in isolation.

\input{tables/ablation.tex}

\subsection{Does a server-side fairness correction add anything on top of BFWA?}
\label{sec:exp-equfl}
A live literature review turned up a very recent (April 2026) server-side
debiasing mechanism, EquFL \cite{yu2026equfl}, which adds a fairness-gradient
correction $\gamma\cdot g_0$ to the aggregated update each round, with $g_0$
the gradient of a fairness surrogate evaluated on a held-out set; the original
paper proves this composes additively with \emph{any} aggregation rule,
including Byzantine-robust ones. We implemented it (\texttt{server\_calib} in
the trainer, using our existing pooled validation split rather than EquFL's
synthetic-data workaround, since our server already has legitimate access to
pooled validation statistics for the FDP-Fair/FedFACT post-processing
baselines) and stacked it on top of BFWA to test whether it improves
TrustFedGNN further. A reduced-round, single-seed probe looked promising---a
clean, monotonic fairness/utility dial on Bail as $\gamma$ increased---but
\emph{did not survive full-protocol, multi-seed validation}. Comparing against
the correct baseline (TrustFedGNN \emph{without} DP, on which the correction is
stacked), the server pass moves AUC/DPD by less than seed noise, in inconsistent
directions: on Bail $0.9876{\to}0.9905$ AUC and $0.0746{\to}0.0707$ DPD (3 seeds);
on \textbf{German, over the full 10 seeds}, it \emph{worsens} mean DPD slightly
($0.0398{\to}0.0486$) for a marginal AUC gain ($0.6336{\to}0.6421$); and it moves
the wrong way on Elliptic ($0.9433{\to}0.9389$ AUC, $0.0135{\to}0.0273$ DPD,
2 seeds). None of these differences is significant. The coherent explanation is that
EquFL's correction targets residual bias left by aggregators with no built-in
fairness mechanism (its own experiments are against FedAvg and unconstrained
robust rules); BFWA's Frank--Wolfe fairness budget already drives that
residual to near zero by convergence, leaving no bias for an additional
correction to fix---so stacking the two adds noise rather than signal. We
report this as a negative result rather than omit it: it is evidence that
FSER+BFWA's built-in fairness mechanism is already sufficient on its own,
without needing an additional server-side correction pass.

\paragraph{Disparity Constraint Slack under Differential Privacy Noise.}
Because BFWA's dual-ascent penalty steers $\sum_k w_k \tilde{\dpd}_k$ toward $\tau$ over privatised client fairness statistics $\tilde{\dpd}_k = \dpd_k + \mathcal{N}(0, \sigma^2)$, a key question (Issue~I4) is whether the injection of Gaussian DP noise causes constraint violation or slack on the true underlying disparity. Table~\ref{tab:bfwa_slack} reports Monte Carlo evaluations across 40 noise realizations (2 seeds $\times$ 20 rounds) on Bail ($K=10, \tau=0.05$). Under $\varepsilon=2.0$, the empirical slack $\big|\sum_k w_k \tilde{\dpd}_k - \sum_k w_k \dpd_k\big|$ is $0.1777 \pm 0.2372$. As the privacy budget relaxes to $\varepsilon=8.0$, the slack contracts monotonically to $0.0442 \pm 0.0653$, remaining below the budget $\tau$ at this sample size. Importantly, the true post-aggregation disparity remains exceptionally low ($0.0159 \ll \tau=0.05$), consistent with BFWA's dual-ascent penalty absorbing finite DP perturbations rather than the budget being exactly enforced.

\input{tables/revision/bfwa_slack.tex}

\subsection{Privacy--utility: FTGD vs.\ full-gradient DP}
\label{sec:exp-privacy}
Figure~\ref{fig:privacy} sweeps the privacy budget $\epsilon$. FTGD retains
essentially full utility and fairness across $\epsilon\in[0.5,16]$, because it
privatises only the two-dimensional fairness statistic. In contrast, DP-FedAvg's
isotropic gradient noise collapses AUC toward chance at small $\epsilon$. The
utility-preservation claim is thus clear; but---as the reviewer of trustworthy
privacy work rightly demands---low utility cost is only half the story. A cheap
mechanism is worthless if it does not actually reduce leakage. We therefore test
the guarantee \emph{adversarially}.

\begin{figure}[t]
\centering
\includegraphics[width=0.92\textwidth]{figures/privacy_bail.pdf}
\caption{Utility (left) and fairness (right) versus privacy budget $\epsilon$ on
Bail. FTGD (TrustFedGNN) is flat across $\epsilon$; DP-FedAvg degrades sharply as
privacy tightens.}
\label{fig:privacy}
\end{figure}

\paragraph{Does the guarantee stop a real attack?}
To show the statistic-level guarantee is meaningful and not merely formal, we
mount a worst-case \emph{attribute-inference} attack on the released fairness
statistic (Table~\ref{tab:privacy-attack}, Figure~\ref{fig:privacy-attack}). The
adversary is an honest-but-curious server that observes the released group means
and knows every node's model prediction and the sensitive attribute of all
\emph{but one} target node; it then performs the optimal ``differencing''
maximum-likelihood guess of the target's attribute, correctly accounting for the
changing group sizes under each hypothesis. On real Bail predictions the result
is stark. Against the \emph{exact} statistic (no DP), the attack recovers the
target's sensitive attribute \textbf{perfectly (100\%)}: an un-noised group mean
is, by construction, a reconstruction oracle. FTGD's Gaussian noise---calibrated
to the deployed budget---drives inference accuracy down to \textbf{$0.51$ at
$\epsilon{=}8$} and $\le 0.51$ for every $\epsilon\le 16$, i.e.\ to
balanced-random ($0.50$). Crucially, this protection is bought with noise on
\emph{two scalars}, which is why (Figure~\ref{fig:privacy}) it leaves utility and
fairness essentially untouched---whereas achieving comparable protection by
noising the full gradient (DP-FedAvg) destroys utility. This is the honest,
demonstrated form of the privacy contribution: FTGD does not make the whole
update private (Section~\ref{sec:ftgd}), but for the object it does protect---the
released fairness statistic---it converts a perfect reconstruction into a coin
flip at negligible cost.

\begin{table}[t]\centering\small
\caption{Attribute-inference attack on the released fairness statistic (Bail,
real predictions). A worst-case differencing adversary infers a target node's
sensitive attribute. The exact release is a perfect oracle; FTGD's noise reduces
inference to balanced-random ($0.500$) at every deployed $\epsilon$.}
\label{tab:privacy-attack}
\input{tables/privacy_attack.tex}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=0.6\textwidth]{figures/privacy_attack.pdf}
\caption{Sensitive-attribute inference accuracy vs.\ privacy budget. Exact
release (no DP) leaks perfectly; FTGD collapses the attack to chance across the
whole $\epsilon$ range, at the negligible utility cost of Figure~\ref{fig:privacy}.}
\label{fig:privacy-attack}
\end{figure}

\paragraph{Comprehensive Privacy Accounting and Update-Level Attack Validation.}
To ground our privacy guarantees against rigorous review (Issue~I1), Table~\ref{tab:dp_accounting} provides the end-to-end analytical R\'enyi Differential Privacy (RDP) accounting converted to $(\epsilon, \delta)$-DP for all benchmark datasets under default training budgets ($z=1.0, \delta=10^{-5}$). Furthermore, Table~\ref{tab:update_attack} evaluates linear attribute inference probes, grouped by client identity, trained directly on transmitted model parameter updates $g_k$. While FTGD closes leakage on the released fairness statistic channel ($1.000 \to 0.500$ attack AUC), unnoised parameter updates preserve an attribute recovery AUC of $0.672$ (client-grouped folds; an earlier ungrouped probe's $0.9051$ was largely measuring client re-identification). Adding full-gradient DP-SGD noise drives update-level inference to $0.340$---below chance---but incurs a crippling $>15\%$ utility drop. This empirical boundary confirms that FTGD provides focused, lightweight statistic-level differential privacy, which operates complementarily alongside cryptographic Secure Aggregation (SecAgg).

\input{tables/revision/dp_accounting.tex}
\input{tables/revision/update_attack.tex}

\subsection{Fairness--utility Pareto frontier}
Figure~\ref{fig:pareto} traces the frontier as the fairness weight $\lambda$
varies, letting an operator select an operating point---an instance of the
human-in-the-loop control our framework exposes.

\begin{figure}[t]
\centering
\includegraphics[width=0.55\textwidth]{figures/pareto.pdf}
\caption{Fairness--utility trade-off as $\lambda$ varies. Down-left is better
(low DPD, high AUC).}
\label{fig:pareto}
\end{figure}

\subsection{Robustness to Byzantine and fairness-poisoning attacks}
\label{sec:exp-robust}
Table~\ref{tab:robustness} reports AUC \emph{and} both fairness metrics (DPD,
EOD) retained under Gaussian, ALIE, and fairness-poisoning attacks with $2/10$
malicious clients; Figure~\ref{fig:robust} sweeps the number of Byzantine
clients. We report attacked fairness explicitly because a fairness-poisoning
attack aims to raise disparity while preserving accuracy---an AUC-only view would
declare a defence successful even as its fairness collapsed. The picture is
genuinely mixed and we report it as such. The clearest signal is
\emph{utility retention across the whole attack suite}: FedAvg and plain BFWA
collapse under the Gaussian attack ($0.51$ and $0.48$ AUC), Krum collapses under
ALIE ($0.71$), whereas \texttt{robust\_bfwa} is the only aggregator that stays
strong under all three ($0.992$/$0.953$/$0.990$). On fairness, plain BFWA is
visibly \emph{captured} by the fairness-poisoning attack---its EOD explodes to
$0.126$ (DPD $0.086$)---while \texttt{robust\_bfwa} holds EOD at $0.024$ by
screening the fabricated updates first. We do \emph{not} claim \texttt{robust\_bfwa}
is best on every cell: under the fairness-poisoning attack Krum attains a lower
DPD/EOD ($0.049$/$0.007$), and under ALIE plain BFWA retains slightly more
utility ($0.991$ vs.\ $0.953$). The honest summary is that \texttt{robust\_bfwa}
uniquely combines all-attack utility retention with fairness comparable to the
best dedicated robust aggregator, rather than dominating every metric. Cells for
collapsed (near-chance) models are daggered, since a constant predictor reports
spuriously low disparity. These are single-seed runs on Bail; error bars and a
broader attack/seed sweep are future work.

\begin{table}[t]\centering\small
\caption{Utility \emph{and} fairness retained under attack ($2/10$ Byzantine
clients, Bail, single seed). AUC (higher = more robust), DPD and EOD (lower =
fairer). Best per attack--metric in bold among non-collapsed models;
$^{\dagger}$ marks near-chance models whose low disparity is an artefact of
constant predictions.}
\label{tab:robustness}
{\footnotesize\setlength{\tabcolsep}{3.5pt}\input{tables/robustness.tex}}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=0.55\textwidth]{figures/robustness_byz.pdf}
\caption{AUC under Gaussian attack versus number of Byzantine clients (of 10).
Robust aggregation retains utility as corruption grows.}
\label{fig:robust}
\end{figure}

\paragraph{Breakdown Point under Adaptive Stealth Fairness Poisoning.}
To delineate the exact boundary of Byzantine resilience (Issues~I4, I5), we evaluate an omniscient adaptive adversary that projects malicious parameter updates within the median client distance radius $\rho=0.85$ while falsifying its reported disparity to $\widehat{\mathrm{DPD}}=0.0$. Table~\ref{tab:adaptive_poisoner} sweeps the Byzantine client fraction $f \in \{0.1, 0.2, 0.3, 0.4\}$ on Bail. When the adversary conceals itself within the benign convex hull, distance-based screening in Krum and robust BFWA degrades ($w_{\text{adv}} \ge 0.85$, DPD rising to $0.125$). Coordinate-wise median, by contrast, holds its disparity across all sweep points ($\mathrm{DPD} \le 0.038$ at $f \le 0.4$) with no utility loss. The reason is structural rather than empirical: the median admits no client-weight vector and consumes no self-reported metadata, so falsifying $\widehat{\mathrm{DPD}}$ cannot influence it \emph{by construction}, and its resilience to the parameter-space component of the attack is bounded by the standard $f < K/2$ breakdown point rather than being unlimited. We deliberately do \emph{not} report an attacker weight share for this rule: $w_{\text{adv}}$ is undefined for an aggregator that assigns no weights, and earlier ``$w_{\text{adv}}=0.000$'' figures for the median were a fallback default in the reporting path, not a measurement (the harness now emits \texttt{NaN} there).

\input{tables/revision/adaptive_poisoner.tex}

\subsection{Uncertainty, calibration, and convergence}
Table~\ref{tab:efficiency} reports ECE, Brier score, and the group-conditional
uncertainty gap on Bail (MC-dropout, $T{=}20$; single seed). TrustFedGNN is
reasonably calibrated (ECE $0.062$, Brier $0.073$) and its group uncertainty gap
is small ($0.006$), so it does not concentrate uncertainty heavily on one group.
We do not claim it is the \emph{best}-calibrated method: the non-fair FedAvg-GAT
is tighter (ECE $0.031$, gap $0.002$), and the fairness/privacy interventions
cost a modest amount of calibration---the honest trade-off. What the table shows
starkly is the opposite failure mode of full-gradient DP: DP-FedAvg is badly
miscalibrated (ECE $0.322$, Brier $0.274$), consistent with its near-chance
accuracy under isotropic noise. Figure~\ref{fig:convergence} shows stable joint
convergence of utility and both fairness metrics; FTGD's clean task pathway
avoids the variance spikes typical of DP-FedAvg.

\begin{figure}[t]
\centering
\includegraphics[width=0.55\textwidth]{figures/convergence.pdf}
\caption{TrustFedGNN convergence on Bail: AUC rises while DPD/EOD fall and
stabilise (the final DPD of $0.066$ sits slightly above the $\tau{=}0.05$ target,
consistent with the surrogate nature of the BFWA constraint).}
\label{fig:convergence}
\end{figure}

\subsection{Scalability and sustainability}
Table~\ref{tab:efficiency} also reports per-round communication. The precise
claim is that FTGD and BFWA add \emph{no} communication over transmitting the
model itself---the privatised statistics and the metric reports are $O(1)$
scalars, so for a \emph{fixed architecture} a TrustFedGNN round costs the same
bytes as a FedAvg round. It does not follow that TrustFedGNN matches a smaller
baseline byte-for-byte: our TrustFedGNN backbone transmits $0.896$\,MB/round
versus $0.225$\,MB for the plain FedAvg-GAT, because the backbone (hidden width,
FSER layers) is larger, not because of the fairness/privacy machinery. On
\emph{compute}, a TrustFedGNN round is more expensive than FedAvg's (FSER's
per-edge similarities and FTGD's second backward pass), by roughly
$2.4$--$2.5\times$ in wall-clock on the large-scale study
(Table~\ref{tab:large-scale}); we report this rather than claim an efficiency
parity that does not hold. The method nonetheless scales to $K{=}20$ clients and
to the $200$k-node Elliptic graph on commodity CPU hardware.

\paragraph{Scaling to millions of nodes.}
\label{sec:scaling}
To verify that the framework is not confined to graphs that fit in memory, we
additionally run it on \textbf{ogbn-products} \cite{hu2020ogb}---an Amazon
co-purchase graph of $2.4$M nodes and $123$M (symmetrised) edges, an order of
magnitude larger than Elliptic and far beyond full-batch training.
TrustFedGNN's three components are all local or $O(K)$ operations, so they
compose directly with standard mini-batch neighbor sampling
\cite{hamilton2017graphsage}: each client trains on sampled subgraphs, FSER
reweights the sampled edges, FTGD privatises the per-batch group statistics,
and BFWA is unchanged. As on Elliptic, ogbn-products carries no protected
demographic attribute, so both its binary target and its sensitive attribute
are documented structural proxies (Appendix~\ref{app:data}): we therefore
report these numbers as evidence that the method \emph{trains and behaves
sensibly at scale}, not as a demographic-fairness claim---that claim rests on
the five real datasets above.

Table~\ref{tab:large-scale} reports all six methods trained end-to-end on the
full $2.4$M-node graph, three clients' worth of neighbour-sampled mini-batches,
for $15$ communication rounds on commodity CPU hardware (a single
container, no GPU). Three findings stand out. First, utility is genuinely
learnable at this scale: FedAvg-GAT, FairSIN, and both TrustFedGNN variants all
converge to AUC $0.86$--$0.89$, confirming that FSER message passing, FTGD
statistic privatisation, and BFWA aggregation compose correctly with
mini-batch sampling rather than degrading into noise. Second,
\textbf{DP-FedAvg's utility collapses to AUC $0.556$}---far below its
performance on the five smaller datasets (Table~\ref{tab:main-auc})---showing
that naïve per-parameter DP noise, tolerable on models with a few thousand
parameters, becomes actively harmful once mini-batch gradient variance is
already high; this is exactly the failure mode FTGD's 2D statistic
privatisation (Section~\ref{sec:ftgd}) is designed to avoid, and TrustFedGNN's
own DP variant confirms it: it loses only $0.001$ AUC ($0.862$ vs.\ $0.861$
without DP) relative to its non-private counterpart at $\varepsilon\approx 8.0$,
versus DP-FedAvg's $0.32$ absolute-AUC collapse. Third,
\textbf{FaVGNN's adversarial training diverges under sampling at this scale}:
after four healthy rounds (AUC rising to $0.83$) its discriminator overwhelms
the encoder and the classifier collapses to a near-constant prediction (final
AUC $0.495$, chance level), with the corresponding DPD/EOD of $0.000$ reported
in Table~\ref{tab:large-scale} being a spurious side effect of that collapse,
not a fairness achievement---we flag this explicitly ($^{\dagger}$) rather
than let a broken run masquerade as a fair one. This is a genuine
generalisation limitation of adversarial debiasing under the noisier,
smaller-batch gradients that mini-batch sampling induces on million-node
graphs, and (to our knowledge) has not been previously reported for FaVGNN;
we view stabilising adversarial FL objectives under sampling as useful future
work rather than a flaw specific to our comparison. TrustFedGNN's per-round
wall-clock is $2.4$--$2.5\times$ FedAvg's (Table~\ref{tab:large-scale},
last column) on this CPU configuration, the price of FSER's edge-reweighted
message passing and BFWA's Frank--Wolfe iterations at this scale; this
overhead is amortised and communication-free (Table~\ref{tab:efficiency}), and
would shrink substantially under GPU-batched sparse operations, which we
leave to future engineering work. Overall, this confirms that the method's
per-client and $O(K)$ server operations impose no barrier to industrial-scale
deployment, while also surfacing genuine scale-dependent failure modes
(naïve DP, adversarial instability) that a purely small-scale evaluation
would have missed.

\begin{table}[t]\centering\small
\caption{Million-node scaling study (ogbn-products, $2.4$M nodes, $123$M
edges, $K{=}3$ clients, $15$ rounds, CPU). $^{\ast}$2026 baseline.
$^{\dagger}$DPD/EOD is a spurious artefact of a collapsed (near-chance)
classifier, not a genuine fairness result---see discussion above.}
\label{tab:large-scale}
\input{tables/large_scale.tex}
\end{table}

\begin{table}[t]\centering\small
\caption{Calibration, uncertainty, and efficiency (Bail). ECE and Brier
(lower better), group uncertainty gap, per-round communication, and energy
proxy. TrustFedGNN matches FedAvg's communication footprint.}
\label{tab:efficiency}
\input{tables/efficiency.tex}
\end{table}

\subsection{External Validity, Heterogeneity, and Baseline Fidelity}
\label{sec:exp-external}

\paragraph{Dirichlet Heterogeneity and Client Scalability Sweep.}
To examine model behaviour under severe non-IID cross-silo partitioning (Issue~I6, Stanford Q7), Table~\ref{tab:dirichlet_sweep} sweeps the Dirichlet attribute skew parameter $\alpha \in \{0.1, 0.3, 0.5, 1.0\}$ across client populations $K \in \{5, 10, 20\}$ on Bail (48 total training runs). Across all 12 combinations of skew and client scale, TrustFedGNN consistently satisfies the target disparity budget $\tau=0.05$, holding $\mathrm{DPD} \le 0.049$. The weight oscillation metric $\Omega_w = \frac{1}{T}\sum_{t=1}^T \|\mathbf{w}^{(t)} - \mathbf{w}^{(t-1)}\|_1$ reflects active dynamic reweighting by BFWA, reaching $1.186$ at $K=10, \alpha=0.3$, compared to static $\Omega_w = 0.000$ for FedAvg.

\input{tables/revision/dirichlet_sweep.tex}

\paragraph{Robustness to Topological Community Graph Partitions.}
Going beyond synthetic Dirichlet partitions, Table~\ref{tab:partition_comparison} compares performance across Uniform IID, Dirichlet ($\alpha=0.3$), and topological community clustering based on Louvain modularity ($K=5$). When client boundaries mirror cohesive social graph sub-communities, TrustFedGNN retains $\mathrm{DPD} = 0.043 \le 0.05$, verifying external robustness when edges cross client boundaries non-uniformly.

\input{tables/revision/partition_comparison.tex}

\paragraph{Composite Trust Score Rank Stability.}
Addressing concerns regarding arbitrary scalar weights in composite trust scoring (Issue~I11, GPT §7, GLM M5), Table~\ref{tab:trust_score_sensitivity} reports a Monte Carlo evaluation across 2,000 perturbed weight vectors. Under uniform $\pm 25\%$ weight variation across utility, fairness, privacy, robustness, and calibration, the rank order remains exceptionally stable with Spearman $\rho_s = 0.965$ (Kendall $\tau = 0.923$), and TrustFedGNN retains Rank~\#1 in \textbf{100.0\%} of trials (and \textbf{98.9\%} under an unconstrained Dirichlet(1) prior).

\input{tables/revision/trust_score_sensitivity.tex}

\paragraph{Centralized Sanity Anchors and Baseline Fidelity.}
To disprove hypotheses of baseline under-tuning (Issue~I10, DeepSeek §3.3), Table~\ref{tab:centralized_sanity} quantifies the federated generalization gap $\Delta_{\mathrm{FL}} = \mathrm{Metric}_{\mathrm{Centralized}} - \mathrm{Metric}_{\mathrm{Federated}}$ across Bail, Credit, and German for GCN and GAT architectures. On the 30,000-node Credit graph, Federated GCN achieves $0.738$ AUC compared to $0.733$ for Centralized GCN ($\Delta_{\mathrm{AUC}} = -0.006$), confirming that the federated baselines are well-tuned and operate at the centralized upper bound.

\input{tables/revision/centralized_sanity.tex}

\paragraph{Sensitivity to Operational and Topological Subgroup Proxies.}
Finally, Table~\ref{tab:proxy_sensitivity} tests fairness generalization (Issue~I9, Stanford Q8) across: (1) primary demographic attributes, (2) topological degree hubs ($S_{\mathrm{hub}} = \mathbb{I}(\mathrm{deg} > \mathrm{median})$), and (3) behavioral feature quantiles ($S_{\mathrm{feat}} = \mathbb{I}(x_0 > \mathrm{median})$). On Bail and Credit, TrustFedGNN substantially suppresses disparity on behavioral feature quantiles ($\mathrm{DPD} = 0.027$ vs $0.113$ on Bail; $0.012$ vs $0.022$ on Credit), demonstrating that its fairness mitigations extend to non-demographic operational risk partitions.

\input{tables/revision/proxy_sensitivity.tex}

\section{Trustworthiness Analysis and Regulatory Compliance}
\label{sec:trust}

\paragraph{Composite trust score.}
Trustworthiness is multi-dimensional: a model that is accurate but unfair, or
fair but privacy-leaking or attack-fragile, is not trustworthy. We summarise a
method by a single interpretable score in $[0,1]$ that aggregates \emph{the
available} normalised sub-scores---utility (AUC), fairness
($1-\tfrac{\dpd+\eod}{2}/F_{\text{ref}}$), privacy ($1-\epsilon/\epsilon_{\max}$,
and \textbf{$0$ when no DP is applied}, so a non-private method is not rewarded
by omission), calibration ($1-\mathrm{ECE}/\mathrm{ECE}_{\text{ref}}$), and
robustness (attack-retained AUC)---via a weighted power mean. The reference
constants ($F_{\text{ref}}{=}0.20$, $\epsilon_{\max}{=}16$,
$\mathrm{ECE}_{\text{ref}}{=}0.20$) and unit weights are stated explicitly and
are configurable. We stress that the ranking is sensitive to these constants and
to the choice of $p$; the composite is a \emph{transparent communication tool},
not decisive evidence of superiority, and the per-axis results
(Tables~\ref{tab:main-auc}--\ref{tab:efficiency}, \ref{tab:robustness}) are the
primary findings. We do not compute this composite over the full baseline
roster, since it needs calibration and per-round communication figures we only
instrumented for a focused subset; Table~\ref{tab:efficiency} reports the
arithmetic-mean ($p{=}1$) composite, calibration included, for four
representative methods on Bail (FedAvg-GAT, DP-FedAvg, TrustFedGNN without DP,
and full TrustFedGNN). The robustness axis is reported separately
(Table~\ref{tab:robustness}) rather than folded in, because it needs a matched
clean/attacked pair.

\paragraph{Contribution-scoring fidelity (FU-Shapley).}
The two-tier aggregator's incentive layer (\S\ref{sec:trust}) scores each
client's update by gradient alignment against a server target
(\emph{FU-Shapley}) rather than the exact, exponential-cost Shapley value, on
the pre-registered hypothesis (H3) that it approximates exact Shapley in both
\emph{ranking} and \emph{allocation}. Table~\ref{tab:shapley_fidelity} reports
the empirical test against 125 probe points ($K{=}5$, 5 seeds): H3 is
\textsc{refuted} on allocation---sign agreement ($73.6\%$) and mean simplex
$L_1$ distance ($0.7554$) both miss their pre-registered targets by a wide
margin---while the weaker ranking-level claim holds (Spearman
$\rho{=}0.6897$). We therefore use FU-Shapley only as a fast, first-order
ranking heuristic ($O(KP)$ vs.\ $O(2^KP)$), not as a faithful value allocation,
and report this negative result rather than the confirmation we had
pre-registered.

\input{tables/shapley_fidelity.tex}

\paragraph{Explainability audit.}
Because FSER retains its per-edge attention, its behaviour is inspectable. We
measure the \emph{attention bias ratio}: the share of attention mass on
cross-group edges divided by their prevalence. A ratio below one---observed for
TrustFedGNN---means the model attends to cross-group edges \emph{less} than
chance, which is direct evidence that FSER does what it is designed to do to the
attention distribution. We are careful about the scope of this claim: it is a
diagnostic of the \emph{mechanism}, not a validated, faithful attribution of
individual predictions, and we do not evaluate it against edge-masking or
counterfactual-removal explainer benchmarks (a limitation, and a natural next
step). Gradient attribution of the demographic-parity gap similarly localises
which input features move the disparity, offering auditors a starting point for
inspection rather than a certified explanation.

\paragraph{Human-in-the-loop control.}
Two budgets are exposed to a human operator: the fairness target $\tau$ (the
operator-chosen cap on the reported client-disparity statistic that BFWA steers
toward---see the scope caveat in Section~\ref{sec:bfwa}) and the privacy budget
$\epsilon$ (enforced by FTGD's accountant for the released statistic). The Pareto
frontier (Figure~\ref{fig:pareto}) turns budget selection into an informed choice
rather than a hidden default, supporting meaningful human oversight.

\paragraph{Regulatory alignment.}
Table~\ref{tab:compliance} offers a \emph{design-level mapping} from
TrustFedGNN's components to relevant EU AI Act articles (10, 13, 14, 15) and NIST
AI Risk Management Framework functions (Govern/Map/Measure/Manage). We are
deliberate that this is a design checklist showing which obligations each
component is \emph{intended to help address}---not a claim of legal compliance,
which depends on system classification, provider/deployer role, jurisdiction,
intended purpose, and a formal conformity assessment we do not perform. Concretely:
raw graphs stay local and $s$ is kept out of the input feature matrix (though, as
noted in Section~\ref{sec:fser}, FSER does use $s$ inside its attention---a
nuance an auditor must weigh); the FSER attention audit and model card support
transparency; the $\tau,\epsilon$ budgets support human oversight; and robust
aggregation plus the DP guarantee speak to accuracy/robustness. The framework
auto-generates a Mitchell-style model card \cite{mitchell2019model}, making these
artifacts reproducible.

\begin{table}[t]\centering\small
\caption{Mapping of TrustFedGNN components to EU AI Act articles and NIST AI RMF
functions.}
\label{tab:compliance}
\input{tables/compliance.tex}
\end{table}

\section{Discussion, Limitations, and Broader Impact}
\label{sec:discussion}

\paragraph{Why FTGD works.}
The privacy result rests on a structural fact rather than a tuning trick: in a
fairness-regularised model the sensitive attribute is a sufficient-statistic
bottleneck---it touches the objective only through $(\mu_0,\mu_1)$. Privatising
a two-dimensional statistic instead of a $|\theta|$-dimensional gradient avoids
the $\sqrt{|\theta|}$ noise scaling that makes DP-SGD impractical on compact
GNNs. The same principle applies to any group-fairness regulariser expressible
through low-dimensional group statistics, so FTGD is reusable beyond GNNs.

\paragraph{Relation to the closest 2025--2026 methods.}
It is worth stating precisely how TrustFedGNN differs from the most recent
neighbours, because superficially several sound similar. F\textsuperscript{2}GNN
and FairGFL are federated fair-graph methods, but the former offers no privacy
and the latter targets \emph{cross-client} performance fairness (equalising
accuracy across silos with imbalanced overlaps) rather than demographic group
fairness---a different objective that our BFWA does not attempt to displace.
FaVGNN is the closest in ambition (federated, graph, group-fair, private), but
it is formulated for \emph{vertical} FL, where clients hold disjoint feature
columns for shared entities; our horizontal cross-silo setting, where clients
hold disjoint entities with the full feature set, is both more common in
inter-bank fraud detection and structurally different, so we can only port
FaVGNN's transferable client-side ideas rather than run it natively. FDP-Fair
and PUFFLE combine privacy with fairness, but privatise the full gradient (or
train with DP-SGD) and then repair fairness by post-processing or regularisation;
FTGD instead \emph{avoids} the full-gradient noise entirely by privatising the
low-dimensional fairness statistic, which is why it retains utility where they
must trade it away. Finally, and decisively, none of these methods addresses
Byzantine robustness: a single malicious silo can corrupt any of them, whereas
TrustFedGNN-Robust screens such updates before enforcing fairness. The
contribution is thus not a marginal gain on one axis but the first coherent
occupation of the intersection of all of them.

\paragraph{Limitations.}
We are deliberately expansive here, because several of these bound the strength
of our claims. (1)~\emph{Scope of the privacy guarantee.} FTGD's
$(\epsilon,\delta)$-DP is over the \emph{released fairness statistic}
$(\tilde\mu_0,\tilde\mu_1)$, not over the whole transmitted update: the
fairness-gradient masks and FSER's use of $s$ leave sensitive-attribute
dependence in the update that we do not privatise (Section~\ref{sec:ftgd}). A
fully update-level sensitive-attribute-DP mechanism is future work, and our
comparison to DP-FedAvg is therefore not like-for-like. (2)~\emph{BFWA is a
surrogate.} The constraint bounds a weighted average of \emph{reported client}
disparities, not the deployed model's DPD, which is a non-linear function of the
aggregate; $\tau$ is an operator target, not a certified global bound, and can
be infeasible---indeed several reported DPD values exceed the default
$\tau{=}0.05$ (Table~\ref{tab:main-dpd}), which we show rather than hide.
(3)~\emph{DPD$\neq$EOD.} We train on a soft demographic-parity term; this does
not guarantee equal-opportunity improvement, and on Elliptic DPD improves while
EOD \emph{worsens} ($0.020\!\to\!0.075$)---a real tension we report. Demographic
parity itself can be an inappropriate criterion when base rates genuinely differ
across groups (recidivism, default), and should be chosen with domain and legal
input rather than assumed. (4)~\emph{Statistical power is uneven across studies, and we say which is which.}
Significance testing \emph{is} reported where seeds permit: paired Wilcoxon
signed-rank tests over shared seeds for the main comparison on German (10 seeds),
Bail and Pokec (5 seeds each, where $p{=}0.0625$ is the Wilcoxon floor)
(reported in Section~\ref{sec:exp-main}), and Wilcoxon tests with Cohen's $d_z$
and bootstrap CIs under Holm--Bonferroni correction for the 10-seed Pokec-z and
Credit SOTA benchmarks (Table~\ref{tab:statistical_significance}, a separate,
independent analysis at a larger seed count). It is
\emph{not} available for the remaining studies, which are run at one to three
seeds by cost: the Credit/Elliptic cells of the main tables ($2$--$3$ seeds), the
$n{=}3$ ablation suite (Table~\ref{tab:ablation_suite}), and the single-seed
large-scale, robustness/poisoning, partition, and calibration studies. For those,
we report means (and standard deviations where $>1$ seed) only, and small
cross-method gaps there should be read as unresolved rather than as differences.
(5)~\emph{Binary $s$}; multi-valued/intersectional attributes are handled only by
one-vs-rest, not exhaustively evaluated. (6)~\emph{Elliptic/ogbn-products
subgroups are temporal/structural proxies}, not protected demographics; those
results are subgroup-disparity analyses. (7)~\emph{Cross-client edges are
dropped} (each silo is an induced subgraph), so this is federated simulation over
a partitioned benchmark, not genuine multi-institution graph learning; inter-silo
links under privacy are future work. (8)~\emph{Adapted baselines.} Several
competitors are non-trivially adapted to our single-graph, star-topology setting
(Appendix~\ref{app:baselines}); their numbers reflect our faithful-as-possible
reimplementations, not necessarily the original papers, and should not be read as
definitive rankings of those methods. (9)~\emph{Robustness assumes a known
corruption count} $f$ and a representative (non-adaptive) attack suite;
misspecified $f$, adaptive attackers with knowledge of the screen, and the
interaction between distance screening and honest non-IID minority clients are
open issues. (10)~\emph{The composite trust score} is sensitive to its reference
constants and weights and is a communication tool, not decisive evidence.
(11)~\emph{Explainability} rests on attention/gradient diagnostics that we have
not validated against faithfulness benchmarks.

\paragraph{Broader impact.}
Fraud, credit, and AML systems make consequential decisions about people.
Enforcing fairness and privacy reduces the risk of discriminatory or
privacy-violating deployment, but no technical fairness metric captures all
notions of justice; $\tau$ should be set with domain and legal input, and the
sensitive-attribute proxies used for auditing must be governed carefully. By
releasing a reproducible, regulation-aligned framework we aim to lower the
barrier to responsible deployment (SDG~9) rather than to certify any particular
system as "fair''.

\paragraph{Future work.}
Several extensions follow naturally. The statistic-privatisation principle
behind FTGD applies to any fairness criterion expressible through
low-dimensional group statistics---equalised odds, sufficiency, calibration
within groups---so a multi-criterion private-fairness framework is within reach.
Handling \emph{multi-valued and intersectional} sensitive attributes beyond the
one-vs-rest reduction, and modelling \emph{cross-silo edges} under privacy
(where an entity's neighbours span institutions), are the two most consequential
graph-specific directions. On robustness, adaptive attackers with full knowledge
of the screening rule motivate a game-theoretic treatment, and combining our
fairness constraint with certified robust aggregation is open. Replacing our
MC-dropout calibration with a distribution-free conformal-prediction layer in
the style of FedWQ-CP \cite{nguyen2026conformal} is an attractive
complementary direction, since it would give the composite trust score a
finite-sample coverage guarantee rather than an asymptotic one. Finally,
moving from a documented temporal proxy on Elliptic to genuine protected
attributes on a consented financial dataset would let the fairness claims
graduate from subgroup-disparity analysis to demographic guarantees.

\paragraph{Reproducibility.}
All datasets are public and downloaded on demand; all randomness is seeded;
privacy accounting, metrics, and aggregation rules are unit-tested in continuous
integration; and every table and figure in this paper is regenerated from logged
runs by a single script. The code, configurations, and logs are released so that
each reported number is traceable to an exact configuration.

\section{Conclusion}
\label{sec:conclusion}

We presented TrustFedGNN, to our knowledge the first framework to integrate
group fairness, differential privacy, and Byzantine robustness for federated
graph neural networks in a single system, together with a trustworthiness
instrumentation layer---predictive uncertainty, attention audits, a composite
trust score, sustainability accounting, and an EU~AI~Act / NIST~RMF design
mapping. Its three components act at different points: FSER suppresses a
targeted pattern of biased message passing, FTGD releases the low-dimensional
fairness statistic under differential privacy at $O(1)$ noise cost (a
statistic-level guarantee, not whole-update DP), and BFWA (with a
Byzantine-robust variant) steers aggregation toward an operator-chosen disparity
target while resisting poisoning, including a fairness-poisoning attack we
introduce. Across five real datasets spanning credit, recidivism, social, and
large-scale crypto domains, TrustFedGNN does not dominate every individual
metric---specialised baselines win specific cells, and we report this
openly---but it is the only method among sixteen baselines to combine
competitive fairness and utility with sensitive-attribute DP and Byzantine
robustness at once, and every reported number is reproducible from released code
and logs. We were candid about the limits: BFWA's constraint is a surrogate for
global fairness rather than a certificate, the privacy guarantee covers the
released statistic and not the full update, demographic parity does not entail
equalised odds, and several studies are few-seed. Future work will tighten these
---update-level sensitive-attribute DP, a certified global-fairness bound,
cross-silo edges under privacy, multi-valued attributes, and adaptive-attacker
robustness---extending trustworthy federated graph learning toward real
deployment.


\backmatter

\bmhead{Supplementary information}
The full source code, configuration files, and experiment logs underlying
every table and figure in this paper are released alongside the manuscript
(see Data availability below).

\bmhead{Acknowledgements}
Not applicable.

\section*{Declarations}

\begin{itemize}
\item Funding: Not applicable.
\item Conflict of interest/Competing interests: The authors declare no competing interests.
\item Ethics approval and consent to participate: Not applicable (no human or animal subjects; all datasets are public, de-identified benchmarks).
\item Consent for publication: Not applicable.
\item Data availability: All datasets used are public (German, Credit, Bail, Pokec via the NIFTY/FairGNN benchmark releases; Elliptic via Kaggle/public release; ogbn-products via the Open Graph Benchmark).
\item Materials availability: Not applicable.
\item Code availability: Code, configuration, and logs are available from the corresponding author upon request and will be released publicly upon acceptance.
\item Author contribution: Q.-V.D. and N.-S.-A.N. conceived the original FedFairGNN framework and preliminary study. All authors contributed to extending the methodology, experiments, and analysis presented in this article, and all authors read and approved the final manuscript.
\end{itemize}

\begin{appendices}

\section{Hyperparameters and training protocol}
\label{app:hyper}
Table~\ref{tab:hyper} lists every hyperparameter used, with the value and the
range we explored. All methods share the backbone, optimiser, and federated
budget; method-specific hyperparameters (adversary learning rate for FairGNN,
$q$ for q-FedAvg, $\beta$-step for FairFed, post-processing for FDP-Fair) use the
values recommended by their originating papers. Seeds are fixed
($\{0,1,2\}$); differentially private runs are intrinsically stochastic, and we
therefore report mean\,$\pm$\,standard deviation.

\begin{table}[h]
\centering\small
\caption{Hyperparameters. ``Explored'' gives the sweep range where applicable.}
\label{tab:hyper}
\begin{tabular}{lll}
\toprule
Hyperparameter & Default & Explored \\
\midrule
GNN layers $L$ & 2 & $\{2,3\}$ \\
Hidden dimension $d_h$ & 64 & $\{32,64,128\}$ \\
Attention heads (FSER/GAT) & 4 & $\{1,4\}$ \\
Dropout & 0.3 & $\{0.2,0.3,0.5\}$ \\
Local optimiser & AdamW & --- \\
Local learning rate $\eta$ & 0.01 & $\{10^{-3},10^{-2}\}$ \\
Weight decay & $10^{-5}$ & --- \\
Local epochs $E$ & 2 & $\{1,2,3\}$ \\
Communication rounds $T$ & 40--60 & per dataset \\
Clients $K$ & 3--10 & $\{3,5,10,20\}$ \\
Partition & Dirichlet($\alpha{=}0.5$) & $\alpha\in\{0.1,0.5,1.0\}$ \\
Fairness weight $\lambda$ & 1.0 & $\{0,0.25,0.5,1,2,4\}$ \\
Fairness budget $\tau$ & 0.05 & $\{0.01,0.05,0.1\}$ \\
DP $\epsilon$ & 8 & $\{0.5,1,2,4,8,16\}$ \\
DP $\delta$ & $10^{-5}$ & --- \\
DP clip $C$ & 1.0 & --- \\
Frank--Wolfe iters $T_{\mathrm{FW}}$ & 20 & --- \\
Dual step $\eta_\mu$ & 0.1 & --- \\
MC-dropout samples & 20 & --- \\
\bottomrule
\end{tabular}
\end{table}

\section{Baseline reimplementation fidelity}
\label{app:baselines}
Five 2025--2026 competitors (FairGFL \cite{fairgfl2026}, FedGraph-Fair
\cite{fedgraphfair2026}, PUFFLE \cite{corbucci2024puffle}, FedFACT
\cite{fedfact2025}, and the PoPETs'25 FairFed variant
\cite{bendoukha2025popets}) were reimplemented from their published algorithm
descriptions rather than from released code (none ship a public
graph-federated implementation). Each paper's mechanism only partially
transfers to this paper's single global-model, star-topology,
Dirichlet-partitioned-single-graph setting; Table~\ref{tab:baseline-fidelity}
states exactly what was kept and what was dropped or approximated for each, so
that no reported number is over-claimed relative to the original method.
FairGFL, FaVGNN, and FDP-Fair are likewise reimplemented (Section~\ref{sec:related});
full fidelity notes for all reimplemented baselines, including the exact
equations reproduced from each source, are maintained alongside the code
(\texttt{docs/BASELINES\_AND\_SOURCES.md}).

\begin{table}[h]\centering\small
\caption{What each 2025--2026 baseline's reimplementation keeps vs.\ drops.}
\label{tab:baseline-fidelity}
\begin{tabular}{p{2.1cm}p{4.6cm}p{4.6cm}}
\toprule
Baseline & Kept & Dropped / approximated \\
\midrule
FairGFL & weight $\propto 1/(1{+}O_i)$ aggregation rule & $O_i$ (multi-graph
node/edge overlap) proxied by sample-count deviation from the mean, since our
setting has one Dirichlet-partitioned graph, not separate per-client graphs \\
FedGraph-Fair & minimax/DRO dual $\lambda$ reweighting high-loss clients &
personalised per-client models mixed via a learned peer-similarity graph
(orthogonal decentralised-communication contribution) \\
PUFFLE & momentum controller auto-tuning the fairness weight toward a target
disparity, + DP-SGD & the third privacy channel (noised group-count sharing
for clients missing a demographic group) -- unneeded here since every client
observes both groups \\
FedFACT & closed-form global+local offset (exact reduction of the paper's
optimum for binary demographic parity) & general multiclass cost-matrix
calibration and the iterative dual-ascent solver \\
PoPETs'25 & degree-2-polynomial FairFed weighting (the FHE-friendly surrogate
itself) & threshold-CKKS secure aggregation and its noise-as-DP analysis
(cryptographic infrastructure, no numeric effect once computed in the clear) \\
\bottomrule
\end{tabular}
\end{table}

\section{Dataset construction and sensitive attributes}
\label{app:data}
\textbf{German, Credit, Bail} follow the standard NIFTY preprocessing: edges are
symmetrised, features are $z$-score standardised, and the sensitive attribute is
excluded from the node feature matrix (so the model cannot read it directly).
Sensitive attributes are gender (German), age (Credit), and race (Bail); targets
are good-credit, no-default, and (non-)recidivism respectively. \textbf{Pokec}
uses region as the sensitive attribute and working-field as the target.
\textbf{Elliptic} is the public Bitcoin transaction graph; illicit transactions
are the positive class, and the unlabelled majority of nodes is retained for
message passing but excluded from loss and metrics. As Elliptic carries no
protected demographic attribute, we construct a transparent operational proxy:
early- versus late-period transactions, split at the median of the $49$
anonymised time steps, so that we study whether an AML model disproportionately
flags one market period---a genuine temporal-drift concern in deployed
compliance systems. We stress this is a subgroup-disparity analysis, not a
protected-demographic claim.

\textbf{ogbn-products} is used for the million-node scaling study
(Section~\ref{sec:scaling}). It carries no protected demographic attribute, so
we construct documented structural proxies rather than treat any column as a
sensitive attribute: the binary target greedily coarsens the 47-way product
category into two balanced super-classes (largest categories assigned to the
positive class until $\approx\!50\%$ of nodes are covered), giving a task a
GNN can genuinely learn while remaining a deterministic function of the
official label---not an arbitrary split; the sensitive attribute is a
high- vs.\ low-degree split at the median node degree (a structural
connectivity subgroup, analogous to core–periphery position in a co-purchase
graph). Training uses neighbor sampling (fan-out $10,5$; batch size $4096$);
the official OGB split is used for train/validation/test. The test split
alone contains $2.2$M nodes, so both per-round monitoring and the final
reported metrics evaluate a fixed random sub-sample of $100$k test nodes
(seeded, so identical across rounds and comparable across methods) rather
than the full test set, keeping evaluation cost from dominating training
cost at this scale.

Clients receive induced subgraphs under a Dirichlet($\alpha$) label partition
with a small per-client class floor that prevents degenerate single-class silos;
each client inherits the global train/validation/test masks for its nodes, and
the global model is evaluated on the pooled test nodes across all clients.

\section{Reproducibility}
\label{app:repro}
All datasets are public and downloaded on demand into a local cache; all
randomness is seeded; the privacy accountant, metrics, and aggregation rules are
covered by an offline unit-test suite run in continuous integration; and every
table and figure in this paper is regenerated from logged runs by a single
script (\texttt{experiments/report.py}). The full experiment matrix---main
comparison, ablation, privacy sweep, fairness--utility Pareto frontier,
Byzantine-robustness study, scalability, and partition-sensitivity---is driven
by a resumable runner (\texttt{experiments/run\_matrix.py}). Code, config, and
logs are released.

\end{appendices}

\bibliography{ref}

\end{document}
